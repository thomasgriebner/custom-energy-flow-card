# Subspec — Englische i18n via HA-Locale (DE/EN)

**Status:** v7 (user-decisions resolved, ready for plan)
**Datum:** 2026-05-15
**Autor:** Brainstorming-Session mit @griebner
**Verlinkte Hauptspec:** [`2026-05-10-custom-energy-flow-card-design.md`](./2026-05-10-custom-energy-flow-card-design.md)
**Berührte ADRs:** keine (existierende ADRs werden nicht geändert)
**Neuer ADR benötigt:** ja → ADR-0023 (Stub in §4.1)

**Iteration-Log:**

- v1 → v2: User-Decisions aus Phase-3-Self-Review eingearbeitet: (F1) `vitest.config.ts` `coverage.include` wird um `src/i18n/**` erweitert; (F2) `T: Translations` wird Feld in `RenderContext` (für `render/`-Module), separat als Parameter für `battery-ring.ts`, `editor.ts`, `editor-list-sections.ts`, `card-helpers.ts`. (F3) `conventions.md` §11+§15 als Doku-Updates ergänzt. (F4) LOC-Regression-Check in Verifikations-Pipeline. (F5) Tool-Coverage-Hinweis für Test-Files präzisiert.
- v2 → v3: Faktische Korrekturen aus Sub-Agent-Pass-1 eingearbeitet: (P1-F1) Bundle-Baseline 61.4 → 60.0 KiB (echte `metrics/kpi-history.json:26037`). (P1-F2) `editor.ts` ist heute 405 LOC, Conventions §3 ≤ 400 ist bestehender Tech-Debt — LOC-Regression-Check nutzt „nicht über 410 LOC wachsen" statt Hard-Limit. (P1-F3) `node-renderer.test.ts` existiert nicht, neuer Test wird in `card-helpers.test.ts`/`battery-ring.test.ts` eingehängt oder als neue Datei explizit aufgenommen. (P1-F4) `icon.test.ts` aus §3.3 gestrichen. (P1-F5) `derive-display-consumers.ts` behält i18n-Import für Default-Consumer-Naming (Line 43, `mapNoneMode`); nur unassignedGroup-Stelle (Line 72) wird sprachneutral — §2.2 präzisiert. (P1-F6) `nodeName()`-Funktion in `node-renderer.ts:213-225` mit 5 DE-Strings explizit erwähnt. (P1-F7) `CARD_NAME` in `const.ts:5` ist bereits Single-Source — bleibt erhalten; `EN.card.name` muss konsistent dazu sein. (P1-F8) `index.ts` `DE`-Import-Removal explizit. (P1-F9) `ha-types.ts:14` `language: string` non-optional — Spec-`HassLocaleView` angepasst auf näher an HA-Type. (P1-F10) `shouldUpdate`-Logik konkret: Locale-Vergleich VOR `return false` in `card.ts:92-99`. (P1-F11) Commit-Reihenfolge: Type-Change vor Renderer-Refactor.
- v6 → v7: User-Decisions zu den 3 USER-DECISION-Findings entschieden. (U1) Hybrid — `home: 'Home'` statt `'Home consumption'` (kürzer wegen Layout-Risiko); andere HA-Energy-Begriffe (Battery/Import/Export/Solar systems/Batteries) bleiben. (U2) HACS-Listing-README-DE akzeptiert für v0.14.0 (Out-of-Scope §6). (U3) `scripts/kpi.mjs:30` `COVERAGE_REQUIRED_LAYERS` wird um `'i18n'` erweitert (Hard-Gate). Plan-Task in Commit 1 ergänzt.
- v5 → v6: Sub-Agent-Pass-5 (Restrisiko + Konsolidierung) — keine neuen AUTO-FIXes. Alle 5 Iterations-Fixes konsistent, Spec ist freistehend lesbar. 3 verbleibende USER-DECISIONs für finale Vorlage gesammelt: (U1) EN-Strings-Längen für Layout, (U2) HACS-Listing-README-Sprache, (U3) KPI-Layer-Liste optional erweitern. Spec ready for user.
- v4 → v5: Conventions+ADR-Findings aus Sub-Agent-Pass-4 eingearbeitet: (P4-F1) §3 Module-Größen-Hinweis: editor.ts ≤ 400 ist Convention, 410 ist Spec-Toleranz für Tech-Debt — präzisiert in §3.5. (P4-F2) Commit-Scope `index` ist nicht in conventions.md §8 Scope-Liste — Commit 3 auf `refactor(card,render,editor)` umbenannt, index.ts-Änderung in Body erwähnt. (P4-F3) §3.2 Walker: `for...of` ist via §1.6 für Side-Effects erlaubt (Test-Assertions sind Side-Effects), Begründungs-Kommentar ergänzt. (P4-F4) §3.1+§3.2 Imports alphabetisch sortiert. (P4-F5) §1.2 Glossar um `chargedByPlaceholder` + `narrowBanner` + `chargedBy` ergänzt. (P4-F6) ADR-0023-Stub: Option E (Browser-Locale `navigator.language`) ergänzt + verworfen. (P4-F7) ADR-0023-Stub: Entscheidung Pkt 5 ergänzt um „erweitert shouldUpdate aus ADR-0011 um Locale-Vergleich". (P4-F8) Test-Pflicht für CARD_NAME ↔ DE.card.name ↔ EN.card.name-Konsistenz in §3.1 ergänzt. (P4-V1) Plan-Discovery-Task Parameter-Count-Verifikation per `grep` in §2.1 ergänzt. (P4-V2) `as Record<string, unknown>`-Cast in §3.2 mit Begründungs-Kommentar (§1.2). (P4-V3) ADR-0023-Negative-Konsequenzen: Default-Naming-Sprachbindung in `mapNoneMode` ergänzt.
- v3 → v4: Auswirkungs-Findings aus Sub-Agent-Pass-2 eingearbeitet: (P2-F1) **BLOCKER:** `de.ts:59` endet mit `} as const` → `typeof DE` ist Literal-Type, `EN: Translations` wäre nicht kompilierbar. `Translations` jetzt via Mapped-Type-Widening definiert (§1.1). (P2-F2) `battery-ring.test.ts:134` ist 10. DE-Importer (Test-File), in §2.1 ergänzt. (P2-F3) `editor.ts` hat heute KEIN `willUpdate` — neuer Lifecycle-Hook wird explizit hinzugefügt (§0.4 + §1.4). (P2-F4) `scripts/kpi.mjs:30` `COVERAGE_REQUIRED_LAYERS` enthält i18n nicht — Hinweis in §3.5: Coverage-Sichtbarkeit ja, Hard-Threshold gilt global. (P2-F5) `editor-list-sections.test.ts` Sprach-Branching-Test in §3.4 ergänzt. (P2-F6) YAML `home.name: 'Hausverbrauch'` in `examples/2-pv-2-batt.yaml:30` — User-Override bleibt DE; Migration-Hinweis in §5.1 ergänzt. (P2-F7) Smoke-Test-Convention: keine sprachabhängigen Assertions, nur strukturelle (§5.4). (P2-F8) `examples/preview-mocks.ts` `buildMockHass` hat kein `locale`-Feld — Plan-Task zum Edit ergänzt (§0.2-Tabelle + §3.5). (P2-F9) `hacs.json` hat keine Versionsangabe — als „verifiziert, keine Action" markiert (§0.2). (P2-F10) HACS-Listing zeigt deutsches README — als USER-DECISION für finale Vorlage (§6). (P2-F12) `_meta.card_bundle_built` im KPI-Snapshot trägt `CARD_VERSION`-Bump (§5.3).

## 0. Zusammenfassung

Die Card unterstützt aktuell ausschließlich deutsche User-facing-Strings (`src/i18n/de.ts`). Spec §1.4 listet Internationalisierung als bewussten v1.x-Kandidaten mit dem Hinweis „trivial weil zentralisiert". Diese Subspec löst genau diesen Punkt auf: eine zweite Sprache (Englisch) wird via Home-Assistant-Locale (`hass.locale.language`) automatisch aktiviert. Kein neues Config-Feld, kein User-Eingriff nötig.

Die Architektur ist eine `resolveT(lang)`-Factory in `src/i18n/index.ts`: Caller (Card, Editor, Renderer-Module) holen sich pro Render-Pass das passende `Translations`-Objekt und reichen es als Parameter durch — kein Modul-Singleton, keine Side-Effects (konsistent mit Spec §11.1 pure-functions-Geist). `EN` wird mit `const EN: Translations = {...}` typisiert, sodass der Compiler 1:1-Struktur zu DE erzwingt.

Locale-Match ist präfix-basiert: `hass.locale.language.startsWith('de')` → DE, alles andere → EN. Damit ist `de`, `de-DE`, `de-AT`, `de-CH` abgedeckt; jeder andere Wert (inkl. `undefined`) fällt auf EN zurück. Bestehende User mit HA-Locale ≠ `de` sehen ab dem Release eine geänderte Sprache — **Breaking Visual Change**, dokumentiert im Changelog (siehe §5.2).

### 0.0 TL;DR — Was der Planer NICHT tun darf

1. ❌ `src/engine/*` anfassen — Engine ist pure, kennt keine Strings.
2. ❌ `src/util/*` und `src/ha/*` mit i18n-Imports versehen — `util/` darf nichts importieren (ADR-0009 Layer-Zone), `ha/` ist HA-Boundary, beide lesen keine Strings.
3. ❌ Neues Config-Feld `display.language` oder ähnliches in `src/config/types.ts` einführen — Sprache kommt ausschließlich aus `hass.locale.language` (siehe §0.4 + §1.2).
4. ❌ Dynamic-Import (`await import('./en.ts')`) verwenden — Bundle-Strategie ist statisch (§1.4).
5. ❌ Modul-Singleton-Pattern (`let currentT = DE; export function setLang(l) {...}`) einführen — `T` lebt als Feld in `RenderContext` (für `render/`-Module) oder als Funktions-Parameter (für `battery-ring.ts`, `editor.ts`, `editor-list-sections.ts`, `card-helpers.ts`), niemals global gestateet.
6. ❌ `src/index.ts` (customCards-Registry) auf `resolveT` umstellen — Registry läuft vor hass-Available, Strings sind dort statisch englisch (siehe §1.3).
7. ❌ Bestehende `DE`-Objekte rename/move — `src/i18n/de.ts` bleibt unverändert, dient als Source-of-Truth für `type Translations = typeof DE`.
8. ❌ Reaktiven Sprach-Switch ohne hass-Update bauen — Lit-Reaktivität via `willUpdate` reicht; kein eigener Event-Bus.
9. ❌ Weitere Sprachen (FR, ES, …) hinzufügen — v1.x-Kandidat (siehe §6).
10. ❌ README-Übersetzung englisch — Doku bleibt deutsch in dieser Spec (siehe §6).
11. ❌ Eigene `Lang`-Type-Definition außerhalb `src/i18n/index.ts` — Single-Source.
12. ❌ `as any` / `as unknown` für `EN`-Konstruktion — wenn Typ nicht passt, ist Struktur falsch (siehe §1.1 Type-Safety).
13. ❌ Test-File für `de.ts` erstellen (`de.test.ts`) — bestehende Renderer/Editor-Tests verifizieren DE-Pfad bereits indirekt; nur EN-spezifische Tests neu (§3.2).
14. ❌ Editor-Schema (`ha-form`-Schema) Sprach-abhängig konstruieren — Editor liest dasselbe `T` wie die Card und gut.

Bei Konflikt zwischen Verbot und Plan-Schritt: STOP und nachfragen.

### 0.1 Harte Constraints für den Planer

**ESLint-Layer-Zonen aus `.eslintrc.cjs`** (authoritative — Spec NICHT doppelpflegen, immer die echte Config lesen):

| Target                                 | Darf importieren aus                                                                                  |
| -------------------------------------- | ----------------------------------------------------------------------------------------------------- |
| `src/engine/`                          | `engine/`, `util/memo.ts`, `util/warning-types.ts`                                                    |
| `src/config/`                          | `config/`, `util/`, `engine/types.ts`, `i18n/`                                                        |
| `src/render/`                          | `render/`, `util/`, `engine/types.ts`, `engine/flow-graph.ts`, `config/types.ts`, `const.ts`, `i18n/` |
| `src/util/`                            | nur `util/`                                                                                           |
| `src/ha/`                              | `ha/`, `config/types.ts`, `engine/types.ts`                                                           |
| `src/i18n/`                            | nur `i18n/` (intern), keine externen Layer                                                            |
| `src/editor.ts`                        | `config/`, `ha/`, `util/`, `i18n/`, `const.ts`, `editor-list-sections.ts`                             |
| `src/editor-list-sections.ts`          | `config/`, `ha/`, `util/`, `i18n/`, `const.ts`                                                        |
| `card.ts`/`index.ts`/`card-helpers.ts` | keine Restricted-Path-Zone — können alle Layer importieren (Top-Level)                                |

**i18n/ darf nichts außer i18n/ importieren** — insbesondere keine `ha/`-Types. Das `Lang`-Resolver-Helfer-Argument ist deshalb `hass: { locale?: { language?: string } } | undefined` (strukturell, nicht `HomeAssistant`-Type), damit `i18n/` Layer-Zone wahrt.

**Weitere Constraints:**

| Constraint                                          | Quelle                | Konsequenz bei Verletzung                                                                                                              |
| --------------------------------------------------- | --------------------- | -------------------------------------------------------------------------------------------------------------------------------------- |
| Engine = pure functions, keine Strings              | ADR-0004, CLAUDE.md 1 | i18n-Import in `engine/` bricht ESLint                                                                                                 |
| `card.ts` ≤ 200 LOC                                 | CLAUDE.md 3           | Lang-Resolution muss kompakt bleiben (siehe §1.2 LOC-Δ)                                                                                |
| Keine `any` ohne Begründungs-Kommentar              | CLAUDE.md 4           | Typisierung `EN: Translations` statt `as any`                                                                                          |
| Berechnung in `willUpdate`, niemals `render`        | CLAUDE.md 5           | `this._lang` wird in `willUpdate` gesetzt, nicht `render`                                                                              |
| Custom `shouldUpdate` für `hass`-Property           | ADR-0011, CLAUDE.md 6 | `shouldUpdate` muss `locale.language`-Change erkennen                                                                                  |
| Crash-Resilient: try/catch + Fallback-UI            | CLAUDE.md 7           | `langFromHass(undefined)` darf nicht crashen, fällt auf EN                                                                             |
| Strings aus `i18n/`, niemals hardcoded              | CLAUDE.md 8           | Kein `'+ PV hinzufügen'` außerhalb `i18n/`                                                                                             |
| TDD für Engine/Util/Config + ≥ 90% Coverage         | CLAUDE.md 9           | `i18n/index.ts` braucht Tests vor Code (§3.2); `vitest.config.ts:coverage.include` wird um `src/i18n/**` erweitert (siehe §0.2 + §3.5) |
| HA-Custom-Elements (`ha-form`, …) NICHT importieren | CLAUDE.md 10          | `editor.ts` bleibt strukturell wie heute                                                                                               |
| Layer-Boundaries via ESLint `no-restricted-paths`   | ADR-0009              | `i18n/` darf keine externen Layer importieren                                                                                          |
| Pre-Release-Smoke-Test grün                         | ADR-0012              | Smoke-Test rendert Card mit Mock-hass, ohne Locale                                                                                     |
| Bundle-Budget ≤ 64 KiB minified                     | ADR-0022              | EN-Datei ~1–2 KiB; aktuell 60.0 KiB (`metrics/kpi-history.json:26037` = 61475 Bytes), erwartet ~61 KiB                                 |
| KPI-Pre/Post-Snapshot                               | ADR-0021, CLAUDE.md   | `pnpm kpi:snapshot --label pre-en-i18n --phase pre` vorab                                                                              |

**Weitere verbindliche Lese-Quellen für den Planer:**

- `CLAUDE.md` (Projekt-Schnellreferenz, Workflow-Regeln, Anti-Patterns)
- `docs/conventions.md` (Code-Stil §11 Anti-Patterns, §15 UX-Sprache)
- `docs/architecture.md` (Module-Map §2, ADR-Tabelle §4)
- Hauptspec §1.4 (Out-of-Scope, der aufzulösen ist) und §11.5 (i18n-Anti-Pattern)
- Alle in obiger Tabelle referenzierten ADRs

### 0.2 Architektur-Kontext (welche Layer berührt)

| Layer     | Datei                                                     | Art der Änderung                                                                                                                                                                                 |
| --------- | --------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `i18n/`   | `src/i18n/de.ts`                                          | check — bleibt unverändert; dient als Source-of-Truth für `type Translations`                                                                                                                    |
| `i18n/`   | `src/i18n/en.ts`                                          | new — `export const EN: Translations = {...}`                                                                                                                                                    |
| `i18n/`   | `src/i18n/index.ts`                                       | new — `type Translations`, `type Lang`, `langFromHass()`, `resolveT()`                                                                                                                           |
| `i18n/`   | `src/i18n/index.test.ts`                                  | new — Locale-Match + Resolver-Tests                                                                                                                                                              |
| `i18n/`   | `src/i18n/en.test.ts`                                     | new — Strukturelle Identität EN ↔ DE + funktionale Strings                                                                                                                                       |
| `card/`   | `src/card.ts`                                             | edit — `@state _lang: Lang`, `langFromHass()` in `willUpdate`, `T` an Renderer                                                                                                                   |
| `card/`   | `src/editor.ts`                                           | edit — analog: `_lang`, `T` an `editor-list-sections` weiterreichen                                                                                                                              |
| `card/`   | `src/index.ts`                                            | edit — `customCards.push({ name, description })` Strings statisch englisch                                                                                                                       |
| `render/` | `src/render/context.ts`                                   | edit — `RenderContext` bekommt `t: Translations` Feld (§1.5)                                                                                                                                     |
| `render/` | `src/render/flow-renderer.ts`                             | edit — `DE`-Import entfernt; nutzt `ctx.t` (Signatur unverändert)                                                                                                                                |
| `render/` | `src/render/node-renderer.ts`                             | edit — `DE`-Import entfernt; nutzt `ctx.t`; löst `node.name ?? ctx.t.nodes.unassignedGroup` auf                                                                                                  |
| `render/` | `src/render/battery-ring.ts`                              | edit — kein `RenderContext` heute → bekommt `T: Translations` als Parameter                                                                                                                      |
| `card/`   | `src/card-helpers.ts`                                     | edit — Helper bekommen `T` als Parameter; `DE`-Import entfernt                                                                                                                                   |
| `card/`   | `src/editor-list-sections.ts`                             | edit — dito (`T` als Parameter; kein `RenderContext`)                                                                                                                                            |
| `config/` | `src/config/types.ts`                                     | edit — `DisplayConsumer.name: string` → `string \| undefined`                                                                                                                                    |
| `config/` | `src/config/derive-display-consumers.ts`                  | edit — `DE`-Import entfernen; unassigned-Group: `name: undefined` setzen                                                                                                                         |
| `util/`   | `src/const.ts`                                            | edit — `CARD_VERSION: '0.13.0' → '0.14.0'`                                                                                                                                                       |
| build     | `vitest.config.ts`                                        | edit — `coverage.include` erweitern: `'src/i18n/**'` (F1) für Coverage-Report-Sichtbarkeit                                                                                                       |
| —         | `docs/adr/0023-i18n-via-hass-locale.md`                   | new — neuer ADR (Stub in §4.1)                                                                                                                                                                   |
| —         | `docs/adr/README.md`                                      | edit — ADR-0023 in Index                                                                                                                                                                         |
| —         | `docs/architecture.md`                                    | edit — §2 Module-Map (`i18n/` Beschreibung), §4 ADR-Tabelle (0023)                                                                                                                               |
| —         | `docs/specs/2026-05-10-custom-energy-flow-card-design.md` | edit — §1.3 In-Scope-Erweiterung, §1.4 i18n-Bullet streichen                                                                                                                                     |
| —         | `CLAUDE.md`                                               | edit — Kritische Regel 8: „Strings aus `i18n/`, niemals hardcoded" (sprachneutral)                                                                                                               |
| —         | `README.md`                                               | edit — Changelog-Eintrag (§5.3); ein Satz in „Was sie kann"                                                                                                                                      |
| —         | `hacs.json`                                               | **check — verifiziert: keine Versionsangabe vorhanden** (nur `name`/`render_readme`/`filename`), keine Synchronisation nötig                                                                     |
| —         | `examples/preview-mocks.ts`                               | **edit** — `buildMockHass()` (`preview-mocks.ts:451`) hat heute KEIN `locale`-Feld; ergänzen um `locale: { language: 'de' \| 'en' }` mit Default `'de'`, damit Sandbox-Sprach-Switch möglich ist |
| —         | `examples/preview.html`                                   | **edit** — UI-Toggle (zwei Buttons „DE" / „EN") zum Umschalten der `locale.language` im Mock-hass, damit Verifikations-Schritt §3.5 #8 ausführbar wird                                           |

**NICHT zu berührende Layer** (Verstoß bricht CI via ESLint `no-restricted-paths`):

- `engine/` — pure Energiebilanz, keine Strings, kein Layer-Cross-Import
- `util/` — Single-Source-Helfer, dürfen nichts importieren (Layer-Zone)
- `ha/` — HA-Boundary-Types, lesen keine Strings; `langFromHass()` nutzt strukturellen Type, kein `HomeAssistant`-Import
- Smoke-Test (`tests/smoke/*`) — falls vorhanden, läuft mit Mock-hass ohne Locale (siehe §0.2.1)

**Single-Source-Regeln (ADR-0010):**

- `type Translations = typeof DE` ist **die einzige** Type-Definition. `EN` wird durch `: Translations`-Annotation auf 1:1-Struktur erzwungen.
- `Lang` (`'de' | 'en'`) ist ausschließlich in `src/i18n/index.ts` definiert. Caller importieren von dort.
- `langFromHass()` ist die **einzige** Stelle, an der `hass.locale.language` interpretiert wird. Caller rufen die Funktion, machen keine eigenen String-Vergleiche.

#### 0.2.1 Files-to-Verify — Parent-Dirs + Tool-Coverage

| Datei (NEW)              | Parent-Dir existiert? | Welche Tools decken sie ab?                           |
| ------------------------ | --------------------- | ----------------------------------------------------- |
| `src/i18n/en.ts`         | ✓ existiert           | `typecheck` + `lint` + `test` (indirekt via Importer) |
| `src/i18n/index.ts`      | ✓ existiert           | `typecheck` + `lint` + `test` (via `index.test.ts`)   |
| `src/i18n/index.test.ts` | ✓ existiert           | Vitest (`pnpm test:coverage` zählt für `i18n/`)       |
| `src/i18n/en.test.ts`    | ✓ existiert           | Vitest                                                |
| `docs/adr/0023-...md`    | ✓ existiert           | keine Tool-Coverage (Markdown)                        |

**Faustregeln:**

- `tsconfig.json:11` excludiert `**/*.test.ts` → Test-Files werden NICHT von `pnpm typecheck` geprüft. Type-Fehler in `en.test.ts`/`index.test.ts` werden erst beim Vitest-Lauf via esbuild sichtbar. Bei `noUncheckedIndexedAccess` (tsconfig) musst du im Test-Walker `b[key]` explizit gegen `undefined` schützen (siehe §3.2 für konkretes Pattern).
- ESLint läuft auf `src/**/*.ts` (laut `package.json` `pnpm lint`) — Test-Files sind included, Layer-Boundaries gelten auch dort.
- `vitest.config.ts:14` `coverage.include` muss in dieser Spec um `'src/i18n/**'` erweitert werden (F1) — sonst keine Coverage-Sichtbarkeit für `i18n/`. Test-Files (`*.test.ts`) sind via `coverage.exclude` ausgeschlossen.
- Smoke-Test (ADR-0012, `scripts/smoke-test.mjs`) rendert die Card mit Mock-hass. Falls Mock kein `locale`-Feld setzt, fällt `langFromHass` automatisch auf EN — kein Test-Bruch erwartet, aber Planer-Pflicht: nach Refactor `pnpm smoke` ausführen.

### 0.3 Konzept-Modell — Datenfluss-Pipeline

Wichtig damit der Planer versteht, **welcher** Schritt durch diese Spec berührt wird:

```
HA hass.states ─┐
                ├─► buildSystemState(config, hass) ─► engine.compute() ─► FlowResult
config         ─┘                                          (pure)              │
                                                                               │
hass.locale.language ─► langFromHass() ─► Lang ─► resolveT() ─► Translations ──┤
                          (NEU, §1.2)             (NEU, §1.1)                  │
                                                                               ▼
                                                              renderCard(layout, flow, T, ctx)
                                                                  (T wird als Parameter durchgereicht)
```

**Pflicht-Wissen:**

- Engine bleibt **vollständig sprachneutral** — keine `T`-Parameter dort, keine String-Outputs.
- `Translations` wird in `card.ts.willUpdate` aus `hass.locale.language` resolved und in `render()` an alle Renderer-/Helper-Aufrufe weitergereicht.
- `derive-display-consumers` (Config-Layer) bleibt sprachneutral — setzt `name: undefined` für die unassigned-Gruppe; der Renderer löst die Default-Bezeichnung gegen `T.nodes.unassignedGroup` auf.
- `customCards`-Registry (`src/index.ts`) läuft **vor** dem ersten `hass`-Update → Strings dort statisch englisch hardcoded (kein i18n-Lookup möglich).

### 0.4 Lit-Lifecycle (`card.ts` / `editor.ts`)

```
setConfig(yaml)     ─► validateConfig ─► this._config = ... (sync)
hass = newHass      ─► shouldUpdate(prev, new)
                         ├─ false: skip
                         └─ true: ↓
                       willUpdate():
                         this._lang = langFromHass(this.hass)   ← NEU
                         buildCardState → engine.compute → computeLayout
                                     Side-effects: this._flowResult = ..., this._layout = ...
                       render():
                         const T = resolveT(this._lang)         ← NEU
                         produces TemplateResult (PURE)
```

**Pflicht-Wissen:**

- `shouldUpdate` (ADR-0011) muss `locale.language`-Änderungen erkennen, sonst rendert die Card bei HA-Sprachwechsel nicht neu. Heutige Implementation in `src/card.ts:92-99` ruft `hassRelevantSensorsChanged(prev, this.hass, this._config)` aus `src/card-helpers.ts:65` und gibt `false` zurück wenn keine relevanten Sensoren geändert. **Locale-Vergleich muss VOR diesem `return false` eingehängt werden**, sonst wird ein reines Locale-Update verschluckt:

  ```ts
  // card.ts:92-99 (heute):
  protected override shouldUpdate(changed: PropertyValues): boolean {
    if (changed.size === 1 && changed.has('hass')) {
      const prev = changed.get('hass') as HomeAssistant | undefined;
      if (!hassRelevantSensorsChanged(prev, this.hass, this._config)) return false;
    }
    return true;
  }
  ```

  ```ts
  // card.ts:92-99 (nachher — ein zusätzlicher Vergleich):
  protected override shouldUpdate(changed: PropertyValues): boolean {
    if (changed.size === 1 && changed.has('hass')) {
      const prev = changed.get('hass') as HomeAssistant | undefined;
      // NEU: Locale-Change MUSS re-render triggern
      if (prev?.locale?.language !== this.hass?.locale?.language) return true;
      if (!hassRelevantSensorsChanged(prev, this.hass, this._config)) return false;
    }
    return true;
  }
  ```

  **Alternativ:** `hassRelevantSensorsChanged` erweitern um Locale-Vergleich. Planer wählt die saubere Variante (vermutlich `shouldUpdate` direkt, weil Locale-Vergleich nicht „Sensor-relevant" ist).

- `_lang` ist `@state()`-Property (Lit-reaktiv) — Wechsel triggert re-render automatisch.
- `render()` bleibt PURE — `resolveT()` ist eine reine Lookup-Funktion, keine Side-Effects.

### 0.5 Don't-Touch-Liste (semantisch korrekt, robust)

| Element                      | Wo                                          | Warum nicht anfassen                                                        |
| ---------------------------- | ------------------------------------------- | --------------------------------------------------------------------------- |
| `DE`-Konstante               | `src/i18n/de.ts`                            | Source-of-Truth für `type Translations`; Struktur unverändert               |
| Engine-Pure-Functions        | `src/engine/*`                              | Sprachneutral, kein Eingriff (CLAUDE.md 1)                                  |
| Config-Schema                | `src/config/schema.ts`, `types.ts`          | Kein neues Feld; nur `DisplayConsumer.name: string \| undefined`-Typ-Change |
| `aria-label` strukturell     | `node-renderer.ts`, `flow-renderer.ts`      | Werte werden lokalisiert, ABER Struktur (`aria-label`-Attribut) bleibt      |
| `prefers-reduced-motion`-CSS | `flow-animation.ts`                         | Sprachneutral                                                               |
| `::part()`-Hooks             | `battery-ring.ts`, `node-renderer.ts`       | Theme-Hooks unabhängig von Sprache                                          |
| Animation, Layout, Memo      | `flow-animation.ts`, `layout.ts`, `memo.ts` | Sprachneutral                                                               |
| `hacs.json` Schema           | `hacs.json`                                 | Nur Versionsangabe synchronisieren (falls hardcoded)                        |
| `examples/2-pv-2-batt.yaml`  | `examples/`                                 | Strukturell unverändert                                                     |

### 0.6 Engine-Warnings statt Throws (Konvention)

Bei dieser Spec wird die Konvention nicht aktiv getriggert (keine Engine-Änderung). Falls beim Refactor ein Edge-Case auftaucht (z.B. `T` ist `undefined` weil hass noch nicht da war): **niemals throwen**, sondern Fallback auf EN. `resolveT(lang)` und `langFromHass(hass)` sind so designt, dass beide Funktionen mit `undefined`-Eingaben sinnvolle Defaults liefern (siehe §1.1).

## 1. Sprach-Resolution

### 1.1 `src/i18n/index.ts` — Resolver-Modul

```ts
import { DE } from './de';
import { EN } from './en';

/**
 * Mapped-Type-Widening:
 * `src/i18n/de.ts` endet mit `as const` → `typeof DE` ist Literal-Type (z.B.
 * `editor.sectionBattery: 'Akkus'`). `EN: typeof DE` mit Wert `'Batteries'`
 * würde TypeError werfen (`'Batteries' is not assignable to '"Akkus"'`).
 *
 * `Widen<T>` ersetzt string-Literale durch `string`, Funktionen bleiben unverändert,
 * Objekte rekursiv. Damit ist `Translations` strukturell kompatibel zu `typeof DE`
 * UND erlaubt jeden anderen string in `EN`.
 */
type Widen<T> = {
  readonly [K in keyof T]: T[K] extends string
    ? string
    : T[K] extends (...args: infer A) => infer R
      ? (...args: A) => R
      : Widen<T[K]>;
};

export type Translations = Widen<typeof DE>;
export type Lang = 'de' | 'en';

/**
 * Strukturelle hass-Sicht — kein HA-Type-Import, damit i18n-Layer-Zone gewahrt bleibt.
 * Hinweis: `src/ha/ha-types.ts:14` hat `locale?: { language: string }` (language non-optional).
 * Wir halten `language` hier OPTIONAL als defensive Verteidigung gegen partielle hass-Objekte
 * vor dem ersten `setHass`-Update (`langFromHass` muss `undefined` tolerieren — siehe Tests in §3.1).
 */
type HassLocaleView = { locale?: { language?: string } } | undefined;

export function langFromHass(hass: HassLocaleView): Lang {
  const lang = hass?.locale?.language;
  if (typeof lang === 'string' && lang.toLowerCase().startsWith('de')) return 'de';
  return 'en';
}

export function resolveT(lang: Lang): Translations {
  return lang === 'de' ? DE : EN;
}
```

**Type-Verifikation (Compiler):**

- `DE` ist `Readonly<{ card: { name: 'Custom Energy Flow Card'; ... }; ... }>` durch `as const`.
- `Widen<typeof DE>` ist `Readonly<{ card: { name: string; ... }; ... }>`.
- `EN: Translations` mit `name: 'Custom Energy Flow Card'` (eigentlich identisch zu DE, aber kein Zwang) und z.B. `nodes.battery: 'Battery'` (≠ DE `'Speicher'`) → **kompiliert**.
- `resolveT(lang)`-Return-Type ist `Translations` (gewidet) — Caller bekommen `string`-Typen, kein Literal-Drift.

**Test-Coverage für das Type-System** (in `i18n/index.test.ts`):

Ein Compile-Zeit-Test reicht nicht via Vitest (Test-Files sind aus `tsconfig.exclude`). Stattdessen Runtime-Assertion:

```ts
// Bei strukturellem Match liefert resolveT('en') ein Objekt mit string-Werten ≠ DE-Werten.
expect(resolveT('en').nodes.battery).not.toBe(DE.nodes.battery);
```

So fällt jede Type-System-Regression nicht stillschweigend durch.

**Type-Safety:**

- `type Translations = typeof DE` macht DE zur Source-of-Truth für die Struktur.
- `src/i18n/en.ts` deklariert `export const EN: Translations = {...}` — fehlende Keys oder falsche Typen brechen `pnpm typecheck`.
- Funktionale Strings (`pluralize`, `pairingMissing`) sind in DE als Funktions-Typen typisiert (siehe `de.ts`); EN muss dieselben Signaturen liefern.

**Locale-Match (präfix-basiert):**

- `'de'`, `'de-DE'`, `'de-AT'`, `'de-CH'` → `'de'`
- `'en'`, `'en-US'`, `'en-GB'`, …, `'fr'`, `'es'`, `'ja'`, `undefined`, `''` → `'en'`
- `.toLowerCase()` ist defensiv (HA liefert üblicherweise lowercase, aber Belt-and-Suspenders).

### 1.2 `src/i18n/en.ts` — Englische Übersetzung

Vollständige 1:1-Struktur-Kopie von DE, alle Strings auf Englisch:

```ts
import type { Translations } from './index';

export const EN: Translations = {
  card: {
    name: 'Custom Energy Flow Card',
    description:
      'Multi-source energy-flow visualization with any number of PVs, batteries and consumers',
  },
  nodes: {
    solar: 'Solar',
    battery: 'Battery',
    grid: 'Grid',
    home: 'Home',
    consumer: 'Consumer',
    unassignedGroup: 'Other',
  },
  units: { watt: 'W', percent: '%' },
  states: {
    loading: 'Loading …',
    sensorUnavailable: 'Sensor unavailable',
    stubHint: 'Add solar, battery or consumer entries to see the energy diagram.',
    cardError: 'Card error',
    narrowBanner: 'Best viewed at 320 px width or more',
  },
  grid: { consumption: 'Import', feedIn: 'Export' },
  battery: { charging: 'charging', discharging: 'discharging' },
  diagnostics: {
    iconLabel: 'Diagnostics',
    title: 'Engine warnings',
    pluralize: (n: number): string => (n === 1 ? 'warning' : 'warnings'),
  },
  editor: {
    sectionGeneral: 'General',
    sectionSolar: 'Solar systems',
    sectionBattery: 'Batteries',
    sectionGrid: 'Grid',
    sectionConsumers: 'Consumers',
    sectionDisplay: 'Display',
    addSolar: '+ Add PV',
    addBattery: '+ Add battery',
    addConsumer: '+ Add consumer',
    chargedBy: 'Charged by',
    chargedByPlaceholder: '— select —',
    pairingMissing: (id: string) => `PV ID "${id}" does not exist`,
    moveUp: '↑',
    moveDown: '↓',
    remove: 'Remove',
    consumerGroupingLabel: 'Consumer grouping',
    consumerGroupingNone: 'None',
    consumerGroupingByArea: 'By HA area',
  },
};
```

**Glossar-Entscheidungen** (verbindlich für den Planer, damit Tonalität konsistent bleibt):

| DE                                 | EN                                  | Begründung                                                                                              |
| ---------------------------------- | ----------------------------------- | ------------------------------------------------------------------------------------------------------- |
| Speicher                           | Battery                             | HA-Standard-Begriff                                                                                     |
| Hausverbrauch                      | Home                                | Verkürzt wegen Layout-Risiko (Node-Label-Overflow); HA-Energy-„Home consumption" wäre langer, hier kurz |
| Bezug                              | Import                              | HA-Energy-Dashboard-Begriff                                                                             |
| Einspeisung                        | Export                              | HA-Energy-Dashboard-Begriff                                                                             |
| Sonstige                           | Other                               | Standard-Englisch für unassigned-Bucket                                                                 |
| Engine-Warnungen                   | Engine warnings                     | technisches Englisch (kein „Issues" — semantisch eng)                                                   |
| Lädt …                             | Loading …                           | Standard                                                                                                |
| Allgemein                          | General                             | Standard                                                                                                |
| „+ PV hinzufügen"                  | „+ Add PV"                          | Imperativ, knapp                                                                                        |
| „— wählen —"                       | „— select —"                        | Editor-Placeholder, Em-Dash beidseits                                                                   |
| Lädt von                           | Charged by                          | Akku-Source-Pairing-Label                                                                               |
| Beste Darstellung ab 320 px Breite | Best viewed at 320 px width or more | Narrow-Container-Banner-Text                                                                            |
| Card-Fehler                        | Card error                          | Fallback-UI bei Render-Crash                                                                            |

### 1.3 `src/index.ts` — customCards-Registry (statisch englisch)

`index.ts` heute (`src/index.ts:1-37`):

```ts
import type { LovelaceCardConstructor } from './ha/ha-types';
import { CARD_DOC_URL, CARD_NAME, CARD_TYPE, CARD_VERSION } from './const';
// ... other imports ...
import { DE } from './i18n/de'; // ← Zeile 4, wird in dieser Spec entfernt
// ...
window.customCards.push({
  type: CARD_TYPE,
  name: CARD_NAME, // ← unverändert, kommt aus const.ts:5 ('Custom Energy Flow Card')
  description: DE.card.description, // ← Zeile 33, wird auf EN.card.description umgestellt
  preview: true,
  documentationURL: CARD_DOC_URL,
});
```

**Nachher:**

```ts
// import { DE } from './i18n/de'; ← Zeile 4 GELÖSCHT
import { EN } from './i18n/en'; // ← NEU
// CARD_NAME bleibt — kommt weiter aus const.ts:5
window.customCards.push({
  type: CARD_TYPE,
  name: CARD_NAME, // unverändert (Single-Source const.ts:5, ADR-0010)
  description: EN.card.description, // statisch englisch, HACS-Norm
  preview: true,
  documentationURL: CARD_DOC_URL,
});
```

**Single-Source-Bedingung (Wert-Konsistenz, kein Compiler-Check):** `CARD_NAME` (`const.ts:5`) trägt den String `'Custom Energy Flow Card'`. Die `card.name`-Felder in `de.ts` und `en.ts` müssen denselben String tragen. Sub-Agent-Pass-Review oder ein One-Liner-Test (`expect(CARD_NAME).toBe(EN.card.name)`) kann das absichern. Aktuell: alle drei Stellen sind bereits `'Custom Energy Flow Card'`.

**Begründung Single-Source erhalten statt CARD_NAME löschen:** `CARD_NAME` wird auch von `src/card.ts` für die LitElement-Klassen-Doku und vom Smoke-Test referenziert (Planer-Discovery). Löschung wäre Scope-Drift.

### 1.4 `T`-Durchreichung — RenderContext + Parameter-Hybrid

**Pattern:** `T: Translations` wird in `src/render/context.ts:RenderContext` als zusätzliches Feld eingehängt — `render/flow-renderer.ts` und `render/node-renderer.ts` lesen es via `ctx.t`, ihre Signaturen ändern sich NICHT. Andere Module ohne `RenderContext` bekommen `T` als separaten Parameter.

```ts
// src/render/context.ts (edit)
import type { Translations } from '../i18n';
// ... bestehende Imports ...

export interface RenderContext {
  config: Config;
  formatGrouped: boolean;
  activeThresholdW: number;
  showInactive: boolean;
  theme: ThemeContext;
  buildWarnings: EngineWarning[];
  unavailableEntities: Set<string>;
  batterySoc: ReadonlyMap<string, number>;
  displayConsumers: ReadonlyMap<string, DisplayConsumer>;
  unavailableGroups: ReadonlySet<string>;
  animation?: AnimationConfig;
  onNodeClick?: (nodeId: string) => void;
  /** Resolved translations (DE oder EN), bestimmt aus hass.locale.language in card.ts. */
  t: Translations;
}
```

**Wer nutzt was?**

| Modul / Datei                 | Mechanismus                                                        | Begründung                                                                     |
| ----------------------------- | ------------------------------------------------------------------ | ------------------------------------------------------------------------------ |
| `src/render/flow-renderer.ts` | `ctx.t.*`                                                          | `RenderContext` ist hier schon Parameter — kein neuer Parameter                |
| `src/render/node-renderer.ts` | `ctx.t.*`                                                          | dito                                                                           |
| `src/render/battery-ring.ts`  | `T: Translations` Parameter                                        | nutzt heute keinen `RenderContext` — kein Refactor des bestehenden Sub-Systems |
| `src/editor.ts`               | lokales `T = resolveT(_lang)` in `render()` + Parameter an Helpers | Editor hat eigene LitElement-Logik, kein `RenderContext`                       |
| `src/editor-list-sections.ts` | `T: Translations` Parameter                                        | bekommt `T` vom Editor                                                         |
| `src/card-helpers.ts`         | `T: Translations` Parameter                                        | bekommt `T` vom Card / Editor                                                  |

**Aufbau von `ctx.t` in `card.ts`:** In `willUpdate` baut `card.ts` den `RenderContext` so wie heute, ergänzt aber `t: resolveT(this._lang)`. `this._lang` ist `@state()` und wurde unmittelbar vorher aus `langFromHass(this.hass)` gesetzt. Lit-Reaktivität triggert re-render bei `_lang`-Wechsel automatisch.

**Anti-Pattern (siehe §0.0 TL;DR Pkt 5):** kein Modul-Singleton `currentT`, kein `setLang()`-Side-Effect. `T` lebt im `RenderContext` (für `render/`) oder als Parameter (für andere Module) — niemals global.

### 1.5 Bundle-Strategie

- DE und EN sind **statisch** im Main-Bundle (`dist/custom-energy-flow-card.js`).
- Keine Dynamic-Imports (`await import('./en.ts')`).
- Erwarteter Bundle-Zuwachs: EN-Strings sind ~10–20 % kürzer als DE → geschätzt +0.8–1.2 KiB minified.
- Aktuell: 60.0 KiB (`metrics/kpi-history.json:26037` `bundle_bytes: 61475`) → erwartet ~61 KiB → klar unter 64 KiB Budget (ADR-0022).
- Verifikation: `pnpm build` nach Implementation, gegen `BUNDLE_BUDGET_BYTES` in `scripts/kpi.mjs:29`.

## 2. Code-Änderungen

### 2.1 Caller-Pattern-Refactor — Hybrid

Siehe §1.4 für die Architektur-Entscheidung. Konkret pro Datei-Gruppe:

**Gruppe A — `render/`-Module mit `RenderContext` (Signatur unverändert):**

```ts
// VORHER (flow-renderer.ts, node-renderer.ts)
import { DE } from '../i18n/de';
// ...
export function renderFlow(layout: Layout, result: FlowResult, ctx: RenderContext) {
  // ...
  const label = DE.nodes.solar;
}

// NACHHER
// kein DE-Import mehr
export function renderFlow(layout: Layout, result: FlowResult, ctx: RenderContext) {
  // ...
  const label = ctx.t.nodes.solar;
}
```

**Gruppe B — Module ohne `RenderContext` (T als Parameter):**

```ts
// VORHER (battery-ring.ts, editor-list-sections.ts, card-helpers.ts)
import { DE } from '../i18n/de';
// ...
export function renderBatteryRing(soc: number | null, color: string): TemplateResult {
  // nutzt DE.battery.charging, DE.units.percent
}

// NACHHER — T als Parameter (Position: bestehende Signaturen erhalten + T als ZUSÄTZLICHEN Parameter)
import type { Translations } from '../i18n';
export function renderBatteryRing(
  soc: number | null,
  color: string,
  t: Translations,
): TemplateResult {
  // nutzt t.battery.charging, t.units.percent
}
```

**Wer ruft `resolveT()` auf?**

- `card.ts.willUpdate()`: setzt `this._lang = langFromHass(this.hass)` (siehe §0.4 Lit-Lifecycle).
- `card.ts` baut den `RenderContext` mit `t: resolveT(this._lang)` als Feld.
- `card.ts.render()` gibt den `RenderContext` an `render/`-Module weiter und ruft für Gruppe-B-Helper direkt `resolveT(this._lang)` (kompakt: einmal in `render()` cachen).
- **`editor.ts` hat heute KEIN `willUpdate`** (verifiziert via `grep -n "willUpdate" src/editor.ts` → null Treffer). Plan-Task: **neues** `willUpdate` in `editor.ts` einführen, das `this._lang = langFromHass(this.hass)` setzt. Alternativ: `setHass`-Setter oder `@state` mit Reaktivität via Property-Comparison. **Empfehlung Planer:** `willUpdate(changed: PropertyValues)` analog zu `card.ts:92` ergänzen, weil Lifecycle-Konsistenz wertvoll ist und Editor-Refactor sowieso ansteht. `editor.ts.render()` ruft `const t = resolveT(this._lang)` und gibt es an `editor-list-sections.ts` und `card-helpers.ts` weiter.

**Parameter-Count-Check (Conventions §1.5 — max. 3–4 Parameter):**

| Funktion (Gruppe B)                 | Parameter heute | Parameter nachher | Über 4? |
| ----------------------------------- | --------------- | ----------------- | ------- |
| `battery-ring.ts` Hauptfunktion(en) | ≤ 3             | ≤ 4 (mit `T`)     | nein    |
| `editor-list-sections.ts` Helper    | ≤ 3             | ≤ 4               | nein    |
| `card-helpers.ts` Helper            | ≤ 3             | ≤ 4               | nein    |

**Falls eine Gruppe-B-Funktion durch `T` auf > 4 Parameter wachsen würde:** Planer verpackt sie in Argument-Objekt (Conventions §1.5). Aktuelle Discovery zeigt aber alle ≤ 3 Parameter heute → mit `T` weiterhin ≤ 4 → kein Argument-Objekt nötig.

**Was `derive-display-consumers.ts` macht:** Config-Layer behält i18n-Import für `mapNoneMode` Default-Naming (Line 43); nur unassignedGroup-Stelle (Line 72) wird sprachneutral. Siehe §2.2 für Details.

**Konkrete DE-Stellen, die der Planer umstellen muss** (Discovery aus Sub-Agent-Pass 1, basierend auf `grep -n "DE\." src/`):

| Datei:Zeile                                                          | Mechanismus nachher                              | Anmerkung                                                                                                                 |
| -------------------------------------------------------------------- | ------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------- |
| `src/render/node-renderer.ts:45`                                     | `ctx.t.states.sensorUnavailable`                 | unavailable-Label                                                                                                         |
| `src/render/node-renderer.ts:213-225` (`nodeName()`)                 | `ctx.t.nodes.{solar,battery,grid,home,consumer}` | 5 DE-Strings, alle in der `nodeName()`-Switch                                                                             |
| `src/render/flow-renderer.ts:*`                                      | `ctx.t.*`                                        | Alle DE-Verwendungen (Diagnostics, etc.) — Planer-Discovery                                                               |
| `src/render/battery-ring.ts:*`                                       | `t.*` (Parameter)                                | aria-label, Soc-% Format                                                                                                  |
| `src/card.ts:*`                                                      | resolveT-Logik + `T` für inline-Strings          | `_lang`-State, willUpdate-Setter                                                                                          |
| `src/editor.ts:*`                                                    | resolveT-Logik + `T`-Durchreichung               | analog zu card.ts (neuer willUpdate, siehe §1.4)                                                                          |
| `src/card-helpers.ts:*`                                              | `t.*` (Parameter)                                | Skeleton/Card-Error-States                                                                                                |
| `src/editor-list-sections.ts:*`                                      | `t.*` (Parameter)                                | Editor-Section-Titel + Buttons                                                                                            |
| `src/index.ts:33`                                                    | `EN.card.description`                            | DE-Import (Line 4) **wird gestrichen**; `CARD_NAME` aus `const.ts:5` bleibt — siehe §1.3                                  |
| `src/config/derive-display-consumers.ts:72`                          | `name: undefined`                                | Line 43 bleibt mit `DE.nodes.consumer` (§2.2)                                                                             |
| `src/render/battery-ring.test.ts:134` (`await import('../i18n/de')`) | Test bleibt DE-basiert                           | Test-Caller, kein Refactor nötig (Test prüft `formatSocPct` mit `DE.units.percent`); analog EN-Variante in §3.4 abgedeckt |

### 2.2 Sprachneutralität in `derive-display-consumers.ts` (nur unassignedGroup-Stelle)

`derive-display-consumers.ts` hat heute **zwei** DE-Stellen:

| Datei:Zeile                      | Verwendung                                                         | Wird in dieser Spec geändert?                                                                                                                                                                                                                                                                                                                                                                                                  |
| -------------------------------- | ------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `derive-display-consumers.ts:43` | `mapNoneMode`: `c.name \|\| \`${DE.nodes.consumer} ${i + 1}\``     | **Nein** — i18n-Import bleibt, Default-Naming „Verbraucher 1/2/3" lokalisiert, aber WIRD WEITERHIN von hier gesetzt mit der HEUTIGEN Sprache. Caller (`card.ts`) ruft `deriveDisplayConsumers(config, …)` mit dem heutigen DE-Default; späterer Sprachwechsel ändert nicht die default-Namen einmal gesetzter Consumer. **Akzeptanz:** ein selten genutzter Edge-Case (User ohne `name` im YAML), Behebung wäre v1.x-Refactor. |
| `derive-display-consumers.ts:72` | `groupByArea`: `unassigned` group `name: DE.nodes.unassignedGroup` | **Ja** — wird `name: undefined`. Der Renderer löst gegen `T.nodes.unassignedGroup` auf, also folgt diese Bezeichnung der aktiven Sprache.                                                                                                                                                                                                                                                                                      |

**Konsequenz:** `derive-display-consumers.ts` **behält** seinen i18n-Import (Line 1: `import { DE } from '../i18n/de'`). Nur **eine** Verwendung (Line 72) wird auf `undefined` umgestellt.

**Vorher (Line 72):**

```ts
return { id: 'g_unassigned', name: DE.nodes.unassignedGroup, members: [...], areaId: undefined };
```

**Nachher:**

```ts
return { id: 'g_unassigned', name: undefined, members: [...], areaId: undefined };
```

**Type-Anpassung in `src/config/types.ts:82`:**

```ts
export interface DisplayConsumer {
  id: string;
  name?: string; // undefined ⇔ unassigned-group (Renderer löst gegen T.nodes.unassignedGroup auf)
  icon?: string;
  members: string[];
  areaId?: string;
}
```

**Renderer-Auflösung** (in `node-renderer.ts` innerhalb `nodeName()` für consumer-kind):

```ts
const label = node.name ?? ctx.t.nodes.unassignedGroup;
```

**Out-of-Scope-Hinweis:** Default-Naming-Sprachwechsel für consumer ohne `name` im YAML (Line 43, `mapNoneMode`) — Behebung würde Daten-Trennung erfordern (Renderer kennt Index nicht, oder Index in `DisplayConsumer.id` codieren). Pragmatischer v1.x-Kandidat, hier nicht adressiert.

### 2.3 Code-Quality-Pflichten + Commit-Granularität

**Pflichten:**

1. Keine neuen Cross-Layer-Imports (ADR-0009 — ESLint bricht CI sonst). `i18n/` darf nichts außer `i18n/` importieren.
2. Keine `any` ohne Begründungs-Kommentar (CLAUDE.md 4). `EN: Translations` ohne Cast.
3. Magic-Strings vermeiden — `'de'`/`'en'` sind durch `Lang`-Type abgedeckt.
4. Engine-Warnings statt Throws (Konvention §0.6).
5. Don't-Touch-Liste respektieren (§0.5).
6. Lit-Lifecycle unangetastet — nur `shouldUpdate` um Locale-Vergleich erweitern und `willUpdate` um `_lang`-Setter (CLAUDE.md 5).
7. Comments synchron halten — wenn `DE`-Strings sich später ändern, MUSS `EN` mitgepflegt werden (Convention; siehe §4.4).

**Commit-Granularität — empfohlen 4 Commits (Reihenfolge bewusst gewählt für Type-Sicherheit):**

| Reihenfolge                                                                                                                                                                                                                                                                                                        | Begründung                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1. `feat(i18n): add resolveT factory + EN translations` (i18n/index.ts, en.ts, vitest.config coverage, +Tests)                                                                                                                                                                                                     | i18n-Infrastruktur in sich abgeschlossen — leicht zu reviewen, isoliert. `pnpm check` grün ohne Caller-Refactor.                                                                                                                                                                                                                                                                                                                                               |
| 2. `refactor(config): make DisplayConsumer.name optional + unassigned-group fix` (types.ts:82 `name?: string`, derive-display-consumers.ts:72 `undefined`)                                                                                                                                                         | Type-Change ZUERST, damit Commit 3 ohne `string \| undefined`-Type-Hole arbeiten kann. Renderer-Fallback noch nicht da → bei `?? T.nodes.unassignedGroup` würde Compiler-Fehler kommen, weil Renderer noch `DE.nodes.*` nutzt. Also: in diesem Commit MUSS auch `derive-display-consumers.test.ts` mit `expect(consumer.name).toBeUndefined()` umgestellt werden.                                                                                              |
| 3. `refactor(render,card,editor): receive Translations via ctx.t or parameter + drop DE imports` (RenderContext, flow-renderer, node-renderer, battery-ring, card.ts, editor.ts, card-helpers.ts, editor-list-sections.ts, **index.ts** DE-Import-Removal + EN-Import, Lit-Lifecycle, shouldUpdate Locale-Trigger) | Caller-Refactor in einem Schritt — alle DE-Imports werden simultan gestrichen, sodass nach diesem Commit keine `DE`-Verwendung mehr außerhalb von `i18n/de.ts` und `derive-display-consumers.ts:43` (mapNoneMode, bleibt) übrig ist. **Scope-Hinweis:** `index.ts` gehört konzeptionell zu `card` (Custom-Element-Registry), conventions.md §8 listet keinen `index`-Scope — daher Multi-Scope `(render,card,editor)` mit `index.ts`-Erwähnung im Commit-Body. |
| 4. `chore(release): bump to 0.14.0 + docs (ADR-0023, hauptspec, CLAUDE.md, conventions §11+§15, architecture.md, README)`                                                                                                                                                                                          | Reine Doku- + Version-Synchronisation                                                                                                                                                                                                                                                                                                                                                                                                                          |

`pnpm check` MUSS zwischen den Commits jeweils grün sein. **Speziell zwischen Commit 1 und 2:** Tests in `i18n/*.test.ts` müssen unabhängig grün laufen (kein Caller braucht `resolveT` noch). **Zwischen Commit 2 und 3:** `derive-display-consumers.test.ts` ist umgestellt, aber der Renderer liest noch `DE.nodes.*` → das passt: der Renderer NUTZT die undefined-Group-Stelle indirekt über `nodeName()`-Fallback erst in Commit 3.

### 2.4 Code-Reuse-Tabelle (verbindlich für Planer)

Vorhandene Helper, die der Planer **wiederverwenden MUSS** statt neu zu schreiben (CLAUDE.md 2 + ADR-0010):

| Helper / Konstante         | Wann verwenden                                                                  | Datei                      |
| -------------------------- | ------------------------------------------------------------------------------- | -------------------------- |
| `DE` (Source-of-Truth)     | Type-Definition (`typeof DE`)                                                   | `src/i18n/de.ts`           |
| `Translations` Type        | Jede Signatur, die ein `T`-Parameter erwartet; `RenderContext.t` Feld-Typ       | `src/i18n/index.ts`        |
| `Lang` Type                | Jede `_lang`-State-Variable, jede Resolver-Signatur                             | `src/i18n/index.ts`        |
| `langFromHass(hass)`       | Einziger Locale-Interpretations-Punkt                                           | `src/i18n/index.ts`        |
| `resolveT(lang)`           | Einziger DE↔EN-Selector                                                         | `src/i18n/index.ts`        |
| `RenderContext.t`          | Translations innerhalb `render/`-Modulen — KEIN neuer `T`-Parameter neben `ctx` | `src/render/context.ts`    |
| `formatPowerW(w, grouped)` | Power-Anzeige (sprachneutral, nutzt Locale für Trennung)                        | `src/util/format-power.ts` |
| `colorFor(role, theme)`    | Farben (sprachneutral)                                                          | `src/render/theme.ts`      |
| `fireMoreInfo(el, entity)` | HA-Event (sprachneutral)                                                        | `src/ha/ha-helpers.ts`     |

**Anti-Patterns, die der Planer aktiv vermeiden muss:**

1. Modul-Singleton (`let _T = DE; export function setLang() {...}`) — verletzt §11.1 pure-functions-Geist.
2. Eigene `Lang`-Type-Definition außerhalb `i18n/index.ts`.
3. `as Translations` Cast — wenn Typ nicht passt, ist die Struktur falsch und MUSS gefixt werden.
4. `if (hass.locale.language === 'de')` ad-hoc im Renderer — immer `langFromHass()` verwenden.
5. `DE`-Re-Import in Caller, der `T`-Parameter hat — alter `DE`-Import MUSS gestrichen werden.
6. Editor-Schema (`ha-form`-Schema) Sprach-conditional konstruieren — Editor liest `T.editor.*` wie die Card.

## 3. Tests

### 3.0 TDD-Reihenfolge

Klassisch nach CLAUDE.md 9 für i18n/ (gilt als „util-nah"):

1. **`i18n/index.test.ts`** schreiben (rot) — alle Cases aus §3.1 Tabelle
2. **`i18n/index.ts`** Skeleton implementieren — Tests grün
3. **`i18n/en.test.ts`** schreiben (rot) — Struktur-Walker und funktionale Strings
4. **`i18n/en.ts`** vollständig schreiben — Tests grün
5. **Renderer/Editor-Refactor** (Commit 2): bestehende Tests passen ggf. mit Parameter-Updates an
6. **`derive-display-consumers`** Test-Update: `expect(name).toBeUndefined()` für unassigned
7. **`pnpm check` + `pnpm build`** verifizieren

### 3.1 Neue Tests — `src/i18n/index.test.ts`

```ts
import { describe, it, expect } from 'vitest';
import { langFromHass, resolveT } from './index';
import { DE } from './de';
import { EN } from './en';

describe('langFromHass', () => {
  it.each([
    ['de', 'de'],
    ['de-DE', 'de'],
    ['de-AT', 'de'],
    ['de-CH', 'de'],
    ['DE', 'de'], // case-insensitive
    ['en', 'en'],
    ['en-US', 'en'],
    ['en-GB', 'en'],
    ['fr', 'en'],
    ['es', 'en'],
    ['ja', 'en'],
    ['', 'en'],
  ] as const)('hass.locale.language=%s → %s', (lang, expected) => {
    expect(langFromHass({ locale: { language: lang } })).toBe(expected);
  });

  it('undefined hass → en', () => {
    expect(langFromHass(undefined)).toBe('en');
  });

  it('hass without locale → en', () => {
    expect(langFromHass({})).toBe('en');
  });

  it('hass.locale without language → en', () => {
    expect(langFromHass({ locale: {} })).toBe('en');
  });
});

describe('resolveT', () => {
  it('resolveT(de) === DE (referential identity)', () => {
    expect(resolveT('de')).toBe(DE);
  });
  it('resolveT(en) === EN (referential identity)', () => {
    expect(resolveT('en')).toBe(EN);
  });
});

// CARD_NAME ↔ DE.card.name ↔ EN.card.name Single-Source-Konsistenz (ADR-0010, P4-F8)
import { CARD_NAME } from '../const';
describe('CARD_NAME Konsistenz', () => {
  it('CARD_NAME === DE.card.name === EN.card.name', () => {
    expect(CARD_NAME).toBe(DE.card.name);
    expect(CARD_NAME).toBe(EN.card.name);
  });
});
```

### 3.2 Neue Tests — `src/i18n/en.test.ts`

```ts
import { describe, it, expect } from 'vitest';
import { DE } from './de';
import { EN } from './en';

/** Rekursiver Walker: jeder Pfad in DE existiert in EN mit gleichem Typ */
function walk(a: unknown, b: unknown, path = ''): void {
  if (typeof a === 'function') {
    expect(typeof b).toBe('function');
    return;
  }
  if (typeof a === 'string') {
    expect(typeof b).toBe('string');
    expect((b as string).length).toBeGreaterThan(0);
    return;
  }
  if (a && typeof a === 'object') {
    expect(b).toBeTypeOf('object');
    expect(b).not.toBeNull();
    // for...of mit Side-Effects (expect-Assertions) ist via conventions §1.6 erlaubt
    for (const key of Object.keys(a)) {
      expect(b).toHaveProperty(key);
      // Internal-Cast (conventions §1.2): walk descends only into Object-Branches,
      // toHaveProperty oben garantiert Key-Existenz vor Index-Zugriff
      walk(
        (a as Record<string, unknown>)[key],
        (b as Record<string, unknown>)[key],
        `${path}.${key}`,
      );
    }
  }
}

describe('EN strukturell identisch zu DE', () => {
  it('jeder DE-Pfad existiert in EN mit gleichem Typ', () => {
    walk(DE, EN);
  });
});

describe('EN funktionale Strings', () => {
  it('pluralize: 0 → warnings, 1 → warning, 2 → warnings', () => {
    expect(EN.diagnostics.pluralize(0)).toBe('warnings');
    expect(EN.diagnostics.pluralize(1)).toBe('warning');
    expect(EN.diagnostics.pluralize(2)).toBe('warnings');
  });
  it('pairingMissing enthält Argument-ID und ist englisch', () => {
    const msg = EN.editor.pairingMissing('foo-1');
    expect(msg).toContain('foo-1');
    expect(msg).toMatch(/does not exist|missing/i);
  });
});
```

### 3.3 Bestehende Tests aktualisieren

**Aktuelle Test-Files in `src/`** (verifiziert via `ls src/**/*.test.ts`):

- `src/card-helpers.test.ts`
- `src/editor-list-sections.test.ts`
- `src/render/battery-ring.test.ts`
- `src/render/icon.test.ts` — nutzt **kein** `DE` (icon.ts ist sprachneutral) → kein Update nötig
- `src/render/layout.test.ts` — sprachneutral → kein Update nötig
- `src/engine/*.test.ts`, `src/util/*.test.ts`, `src/config/*.test.ts` — sprachneutral / via Mock-`T`

| Datei                                            | Änderung                                                                                                                                                                                                                 |
| ------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `src/render/battery-ring.test.ts`                | Renderer-Aufrufe bekommen `T = DE` als Parameter — strukturelle Anpassung                                                                                                                                                |
| `src/config/derive-display-consumers.test.ts`    | unassigned-Gruppe: `expect(consumer.name).toBeUndefined()` statt `'Sonstige'`; default-Consumer-Name (Line 43, mapNoneMode) bleibt mit `DE.nodes.consumer` → bestehende Tests dazu unverändert                           |
| `src/editor-list-sections.test.ts`               | `T = DE` als Parameter durchreichen                                                                                                                                                                                      |
| `src/card-helpers.test.ts`                       | `T = DE` als Parameter durchreichen (für Skeleton/Card-Error-States)                                                                                                                                                     |
| `src/render/flow-renderer.*` / `node-renderer.*` | **Existieren noch nicht als Test-Files** — bestehende Verifikation erfolgt über `pnpm smoke` (ADR-0012) und Sandbox. Bei Refactor: kein neuer Test-Aufwand nötig, weil Renderer-Logik via `card.ts`-Smoke abgedeckt ist. |

### 3.4 Optionale defensive Coverage — Sprach-Branching

Da `src/render/node-renderer.test.ts` **nicht existiert** und in dieser Spec auch nicht angelegt wird, hängen wir den it.each-Sprach-Test in `src/card-helpers.test.ts` ein. `card-helpers.ts` enthält `renderSkeleton`/`renderCardError` mit i18n-relevanten Strings — gleiches Coverage-Ziel, kein neuer Test-File-Aufwand:

```ts
// src/card-helpers.test.ts (neuer Test-Block innerhalb bestehender Suite)
import { describe, it, expect } from 'vitest';
import { resolveT } from './i18n';
import { renderSkeleton } from './card-helpers';

describe('renderSkeleton — Sprach-Branching', () => {
  it.each(['de', 'en'] as const)('rendert i18n-Text in %s', (lang) => {
    const T = resolveT(lang);
    const result = renderSkeleton(T);
    // result ist Lit TemplateResult — strings/expression-values prüfen
    expect(JSON.stringify(result)).toContain(T.states.loading);
  });
});
```

Schließt die Lücke „Sprach-Branching im Renderer aktiv durchlaufen". Ein `it.each` reicht — bestehende Tests für `renderSkeleton` mit DE bleiben unverändert.

**Analoger Test für Editor-Module** in `src/editor-list-sections.test.ts`:

```ts
describe('renderSection — Sprach-Branching', () => {
  it.each(['de', 'en'] as const)('rendert Section-Titel in %s', (lang) => {
    const T = resolveT(lang);
    const result = renderSection(T, /* sectionId */ 'solar', /* config */ {...});
    expect(JSON.stringify(result)).toContain(T.editor.sectionSolar);
  });
});
```

Konkrete Funktions-Signatur hängt von `editor-list-sections.ts`-API ab (Planer wählt einen Test-Punkt mit `T.editor.*`-Verwendung).

### 3.5 Verifikations-Pipeline (Reihenfolge verbindlich)

| #   | Befehl                                                | Erwartung                                                                                                                                                                                                                                                                                                                   |
| --- | ----------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 0   | `pnpm kpi:snapshot --label pre-en-i18n --phase pre`   | KPI-Baseline vor Implementation (ADR-0021, CLAUDE.md Phase 0)                                                                                                                                                                                                                                                               |
| 1   | `pnpm typecheck`                                      | strict grün, `EN: Translations`-Annotation erzwingt 1:1-Struktur                                                                                                                                                                                                                                                            |
| 2   | `pnpm lint`                                           | ESLint grün, Layer-Boundaries eingehalten                                                                                                                                                                                                                                                                                   |
| 3   | `pnpm test`                                           | Vitest grün, neue `i18n/`-Tests grün                                                                                                                                                                                                                                                                                        |
| 4   | `pnpm test:coverage`                                  | Coverage ≥ 90 % global (vitest threshold) UND `i18n` Hard-Gate via KPI (`scripts/kpi.mjs:30` wird in Commit 1 auf `['engine', 'config', 'util', 'i18n']` erweitert — siehe §0.2).                                                                                                                                           |
| 5   | `pnpm check`                                          | Komplettes CI-Gate vor jedem Commit                                                                                                                                                                                                                                                                                         |
| 6   | `pnpm build`                                          | Bundle ≤ 64 KiB minified (erwartet ~61 KiB, +~1 KiB für EN-Strings)                                                                                                                                                                                                                                                         |
| 7   | `wc -l src/card.ts src/editor.ts`                     | **LOC-Regression-Check** (Conventions §3): `card.ts` heute 178 LOC, darf nicht über 200 wachsen. `editor.ts` heute 405 LOC (bestehender Tech-Debt — Conventions §3 sagt ≤ 400, aber wird nicht in dieser Spec adressiert), darf nicht über 410 wachsen. Bei Überschreitung: Lang-Resolution in `card-helpers.ts` auslagern. |
| 8   | `pnpm preview`                                        | Sandbox: Mock-`hass.locale.language` auf `'de'` und `'en'` umschalten, beide Sprachen rendern                                                                                                                                                                                                                               |
| 9   | `pnpm smoke` (ADR-0012)                               | Smoke-Test grün — Mock-hass ohne Locale fällt korrekt auf EN                                                                                                                                                                                                                                                                |
| 10  | `pnpm kpi:snapshot --label post-en-i18n --phase post` | KPI-Snapshot nach Implementation                                                                                                                                                                                                                                                                                            |
| 11  | `pnpm kpi:report`                                     | Delta pre→post: Bundle/LOC/Coverage im erwarteten Rahmen                                                                                                                                                                                                                                                                    |

**Manueller Smoke-Test** (optional, vor Release):

1. Card im HA mit `Profile → Language: Deutsch` öffnen → DE-Strings sichtbar.
2. HA-Sprache auf `English` umstellen → bei nächstem hass-Update sind Strings englisch (ggf. Browser-Refresh nötig, abhängig von HAs Reload-Verhalten).
3. HA-Sprache auf `Français` → englische Strings (Fallback).

## 4. Dokumentation

### 4.1 ADR-0023 Neu — Stub für den Planer

Neuer ADR `docs/adr/0023-i18n-via-hass-locale.md`. **Stub unten 1:1 übernehmen** und in den ADR-Index (`docs/adr/README.md`) eintragen.

```markdown
# ADR-0023: i18n via HA-Locale (DE/EN) mit resolveT-Factory

- **Status:** accepted
- **Datum:** 2026-05-15
- **Entscheider:** @griebner

## Kontext und Problem

Hauptspec §1.4 listet Internationalisierung als bewussten v1.x-Kandidaten („trivial weil zentralisiert"). Für eine runde v1.0 soll Englisch als zweite Sprache verfügbar sein, damit die Card außerhalb des deutschsprachigen Raums (HACS-Reichweite) nutzbar ist.

Drei Architektur-Fragen:

1. Woher kommt die Sprache? Config-Feld oder HA-Locale?
2. Wie holen Caller das Translation-Objekt? Modul-Singleton oder Parameter-Durchreichung?
3. Wie wird die EN-Datei type-sicher 1:1 zur DE-Datei gehalten?

## Entscheidung

**Gewählt:**

1. **Sprache aus `hass.locale.language`** (präfix-basiert: `de*` → DE, alles andere → EN). Kein Config-Feld.
2. **`resolveT(lang)`-Factory** in `src/i18n/index.ts`. Caller halten `_lang` als `@state`, holen `T = resolveT(_lang)` pro Render und reichen `T` als Parameter an Sub-Module weiter (oder via `RenderContext.t` für `render/`-Module). Kein Modul-Singleton.
3. **`type Translations = Widen<typeof DE>`** mit Mapped-Type-Widening (`as const` in `de.ts` macht `typeof DE` literal; Widen-Helper ersetzt Literale durch `string`). `EN: Translations` erzwingt 1:1-Struktur via TypeScript-Compiler.
4. **Bundle-Strategie statisch**: beide Sprachen im Main-Bundle (~+1 KiB), kein Dynamic-Import.
5. **Lit-`shouldUpdate`** aus [ADR-0011](./0011-shouldupdate-over-property-haschanged.md) wird um Locale-Vergleich erweitert (`prev.locale.language !== this.hass.locale.language` triggert re-render zusätzlich zur bestehenden Sensor-Filter-Logik).

### Positive Konsequenzen

- Kein User-Eingriff nötig — Card folgt HA-Sprache automatisch.
- Pure-functions-Geist (§11.1) bleibt erhalten — `T` ist Parameter, kein globaler State.
- TypeScript-Compiler verhindert struktur-Drift zwischen DE und EN.
- Bundle bleibt klar unter 64 KiB (ADR-0022).
- Weitere Sprachen (FR, ES, …) sind nach v1.0 trivial: zusätzliche Datei, `Lang`-Type erweitern, `resolveT`-Branch ergänzen.

### Negative Konsequenzen

- **Breaking Visual Change**: User mit HA-Locale ≠ `de` sehen ab Release Englisch. Dokumentiert im Changelog.
- Keine User-Override-Möglichkeit (kein `display.language`-Feld). Bei Bedarf v1.x-Patch.
- Sprach-Switch erfordert hass-Update (kein eigener Event-Bus) — in der Praxis kein Problem, da HA-Sprachwechsel typischerweise Reload triggert.
- **Default-Knoten-Namen aus `derive-display-consumers.ts:43` (`mapNoneMode`) bleiben sprachgebunden zum Zeitpunkt der Config-Auswertung** — Consumer ohne `name` im YAML bekommen den Default-Namen in der aktiven Sprache; bei späterem Sprachwechsel ändert sich der Default nicht (bewusst akzeptiert, v1.x-Refactor möglich).

## Geprüfte Optionen

- **A — HA-Locale-driven + resolveT-Factory** (gewählt)
- **B — Config-Feld `display.language`** — mehr User-Kontrolle, aber Editor-Feld nötig, weniger automatisch
- **C — Modul-Singleton mit `setLang()`** — kürzer in Callern, aber stateful Module = Anti-Pattern (§11.1)
- **D — Dynamic-Import lazy per Sprache** — minimal kleineres Bundle, aber Async-Komplexität ohne Nutzen bei < 5 KiB Differenz
- **E — Browser-Locale (`navigator.language`) als Fallback** — verworfen, weil Browser-Locale ≠ HA-User-Locale (Server-Time-Zone, Geräte-Locale ≠ User-Präferenz, Multi-User-Setups). HA-Locale ist die Single-Source-of-Truth für die Spracheinstellung des HA-Profils.

## Verlinkte Spec-Sektionen / Referenzen

- Subspec [`docs/specs/2026-05-15-en-i18n.md`](../specs/2026-05-15-en-i18n.md)
- Hauptspec §1.4 (Out-of-Scope, aufgelöst durch diese Entscheidung)
- ADR-0010 (Single-Source-Util-Modul — strukturell analog für `i18n/`)
- ADR-0011 (`shouldUpdate` über `hasChanged` — Locale-Vergleich wird ergänzt)
- ADR-0022 (Bundle-Budget 64 KiB — eingehalten)
- [HA Frontend: Locale](https://developers.home-assistant.io/docs/frontend/data/)
```

### 4.2 Hauptspec-Updates (`docs/specs/2026-05-10-...`)

**§1.3 (In Scope) — neuen Bullet aufnehmen:**

```diff
+ - **Mehrsprachig**: Card und Editor folgen `hass.locale.language` (`de*` → Deutsch,
+   alles andere → Englisch). Kein Config-Eingriff nötig.
```

**§1.4 (Out of Scope) — Bullet streichen oder als aufgelöst markieren:**

```diff
- - Internationalisierung (deutsche Default-Strings, mit `name`-Override pro
-   Knoten in der Config; alle Strings sind aber bereits zentral in `i18n/de.ts`
-   abgelegt — siehe §2.7 — sodass v1.x-i18n trivial wird)
+ - ~~Internationalisierung~~ — aufgelöst durch Subspec
+   [`2026-05-15-en-i18n.md`](./2026-05-15-en-i18n.md) (DE/EN; weitere Sprachen
+   bleiben v1.x-Kandidat).
```

### 4.3 `CLAUDE.md` Update

Kritische Regel 8 sprachneutral formulieren:

```diff
- 8. **Strings aus `i18n/de.ts`.** Niemals user-facing Strings hardcoded in
-    Templates. (Spec §11.5)
+ 8. **Strings aus `i18n/`.** Niemals user-facing Strings hardcoded in
+    Templates. Caller holen `T = resolveT(lang)` und reichen `T` durch.
+    (Spec §11.5, Subspec 2026-05-15-en-i18n)
```

### 4.4 `docs/architecture.md` Updates

1. **§2 Modulkarte** — `i18n/`-Beschreibung erweitern:

   ```diff
   - i18n/    ←  alle User-Strings (DE)
   + i18n/    ←  alle User-Strings (DE + EN); resolveT(lang) Factory + langFromHass(hass)
   ```

2. **§4 ADR-Tabelle**: ADR-0023 hinzufügen.

### 4.5 `docs/conventions.md` Updates (§11 + §15 + neue Pflege-Convention)

Drei Stellen in `docs/conventions.md` aktualisieren:

**§11 Anti-Patterns — Bullet anpassen:**

```diff
- - ❌ Hardcoded User-Strings (immer aus `i18n/de.ts`)
+ - ❌ Hardcoded User-Strings (immer aus `i18n/`, via `resolveT(lang)` aufgelöst — Modul-Singleton verboten)
```

**§15 Sprache — User-facing-Strings-Regel erweitern:**

```diff
- - **User-facing Strings**: Deutsch (in `i18n/de.ts` zentralisiert)
+ - **User-facing Strings**: Deutsch (`i18n/de.ts`) + Englisch (`i18n/en.ts`), Sprach-Auswahl
+   automatisch über `hass.locale.language` (`resolveT(lang)`-Factory in `i18n/index.ts`,
+   siehe ADR-0023). Default-Fallback: EN.
```

**§15 (oder §12 Doku-Pflicht) — neuer Pflege-Hinweis (Planer wählt passende Sektion):**

```markdown
**i18n-Pflege:** Bei jeder Änderung an `src/i18n/de.ts` MUSS `src/i18n/en.ts`
synchron mitgepflegt werden. TypeScript verhindert struktur-Drift
(`EN: Translations`), aber Wert-Drift („DE-Bullet hinzugefügt, EN vergessen")
ist Reviewer-Pflicht. `pnpm typecheck` fängt fehlende Keys.
```

## 5. Migration und Breaking Changes

### 5.1 Was bestehende User sehen

| Bisheriges `hass.locale.language` | Neue Darstellung     | Bruch?                                |
| --------------------------------- | -------------------- | ------------------------------------- |
| `de`, `de-DE`, `de-AT`, `de-CH`   | DE wie bisher        | nein                                  |
| `en`, `en-US`, `en-GB`            | EN — neu             | **ja**, Breaking Visual Change        |
| `fr`, `es`, `it`, `ja`, …         | EN — neu (vorher DE) | **ja**, Breaking Visual Change        |
| `undefined`                       | EN (Fallback)        | minimal, durch hass-Update korrigiert |

Datenmapping bleibt 1:1, alle Sensoren und Edges identisch. Nur User-facing-Strings sind betroffen.

**Edge-Case YAML-`name`-Overrides:** Wenn ein User in seiner Lovelace-Config explizit deutsche Namen vergeben hat (z.B. `home.name: Hausverbrauch` wie in `examples/2-pv-2-batt.yaml:30`), bleibt diese Bezeichnung in der UI **deutsch**, auch wenn die Card auf EN umschaltet. Der Renderer-Fallback in `node-renderer.ts:223` (`ctx.config.home?.name ?? ctx.t.nodes.home`) bevorzugt User-Override. Sub-User mit Mischsprachen-UI: bewusst akzeptiert, kein Bruch (Spec-§5.2 unten).

### 5.2 Was _nicht_ bricht

- Config (`type`, `solar`, `battery`, `consumers`, `display`) bleibt strukturell unverändert.
- HA-Entity-Mapping (kein Sensor-Rename).
- Engine-Output (Flow-Werte, Warnings).
- Animation, Layout, Theme, `::part()`-Hooks.
- Consumer-Grouping (ADR-0016).
- `aria-label`-Struktur (nur Inhalt lokalisiert).

### 5.3 Release-Strategie

- Minor-Bump: `0.13.0 → 0.14.0` (`src/const.ts:CARD_VERSION` und `package.json:version`).
- `hacs.json`: **keine Versionsangabe vorhanden** (verifiziert in §0.2), keine Synchronisation nötig.
- KPI-Pre/Post-Snapshot (`pnpm kpi:snapshot --label pre-en-i18n --phase pre` und `--label post-en-i18n --phase post`) erfasst `_meta.card_bundle_built` aus `dist/custom-energy-flow-card.js`-Header — bei Post-Snapshot ist das `0.14.0`. Delta sichtbar via `pnpm kpi:report`.

**README-Changelog-Eintrag-Vorlage:**

```markdown
## 0.14.0 — 2026-05-15

### Englische Sprachunterstützung

- Card und Editor folgen jetzt der Home-Assistant-Sprache (`hass.locale.language`).
- `de` (inkl. `de-DE`, `de-AT`, `de-CH`) → deutsche Texte wie bisher.
- Alle anderen Sprachen → englische Texte.
- Kein Config-Eintrag nötig — wechselt automatisch beim HA-Sprachwechsel.

### Breaking Visual Change

User mit einer Home-Assistant-Sprache ≠ `de` sehen die Card ab v0.14.0 auf
Englisch (vorher Deutsch). Wer beim alten Verhalten bleiben möchte, kann
in den HA-Profil-Einstellungen die Sprache auf Deutsch stellen.

### Was zu tun ist nach Update

1. **Browser-Cache leeren** (Strg+Shift+R), falls die alte Sprache noch erscheint.
2. Keine Config-Anpassung nötig.
```

HACS-Release-Notes verwenden denselben Text.

### 5.4 Operationelle Aspekte

**Rollback:** rein additiv. Rollback = `git revert` der vier Commits aus §2.3. Configs bleiben kompatibel — kein Daten-Rollback nötig.

**Pre-Release-Gate (ADR-0012):** Headless-Smoke-Test rendert Card mit Mock-hass (`scripts/smoke-test.mjs:69-79` baut `card.hass = { states: {...} }` ohne `locale`-Feld). Konsequenz: `langFromHass(this.hass)` fällt auf EN-Default. **Convention für Smoke-Test:** _keine sprachabhängigen String-Assertions_ — nur strukturelle Checks (Selector, Attribute, `data-*`). Aktueller Smoke-Test erfüllt das bereits (Assertions sind Custom-Element-Registration + render-no-throw). **Planer-Pflicht:** beim ersten Test-Lauf nach Refactor `pnpm smoke` durchlaufen lassen + ggf. neue Assertions auf diese Convention prüfen.

**Performance:** kein Engine-Eingriff, kein Layout-Eingriff. `resolveT(lang)` ist O(1) Lookup. Bundle-Overhead: ~+1 KiB minified.

**Pflege-Aspekt:** zukünftige String-Änderungen MÜSSEN beide Files (`de.ts`, `en.ts`) updaten. TypeScript-Compiler fängt strukturelle Drift; Wert-Drift („englischen String vergessen zu aktualisieren") ist Reviewer-Pflicht (§4.5 Convention-Eintrag).

## 6. Out of Scope

- Weitere Sprachen (FR, ES, IT, …) — v1.x-Kandidat. Pattern ist nach v1.0 trivial erweiterbar: zusätzliche Datei, `Lang`-Type erweitern, `resolveT`-Branch.
- Config-Override für Sprache (`display.language`) — v1.x-Kandidat falls Bedarf besteht.
- Dynamic-Import / Lazy-Loading der Sprachdateien — Bundle-Budget reicht statisch.
- README-/Doku-Übersetzung — bleibt deutsch in dieser Spec. Internationale README ist v1.x-Kandidat. **Bewusst akzeptiert:** HACS-Listing rendert `README.md` (siehe `hacs.json:render_readme: true`), d.h. englische User sehen ein deutsches README. Mitigations-Optionen für v1.x: (a) zweisprachiges README, (b) eigenständige `README.en.md` mit Link, (c) HACS-Description ausführlicher gestalten. **USER-DECISION** in finaler Vorlage falls Priorität hochgestuft.
- Übersetzung der `customCards`-Registry — statisch englisch, kein i18n-Lookup möglich vor hass-Available.
- Default-Knoten-Namen im Editor („Solar 1", „Battery 1"): bestehende `name`-Override-Logik bleibt; auto-generierte Defaults gibts nicht.
- Reaktiver Sprach-Switch ohne hass-Update — Lit-Reaktivität via `willUpdate` reicht.

## 7. Offene Fragen

Keine. Alle Architektur-Entscheidungen und Migrations-Details sind im Brainstorming geklärt.

## 8. Nächster Schritt

Implementations-Plan in `docs/plans/2026-05-15-en-i18n.md` mit Checkbox-Liste. Workflow gemäß CLAUDE.md „Plan-Erstellung":

1. Phase 1 — Repo-Discovery (Plan-Schreiber liest diese Subspec + alle in §0.2 gelisteten Files + Goldstandard-Plan `docs/plans/2026-05-12-aspect-ratio-redesign.md`)
2. Phase 2 — Plan-Schreiben via `superpowers:writing-plans`-Skill (Template `docs/templates/plan-template.md`)
3. Phase 3 — Self-Review gegen `docs/templates/plan-review-checklist.md`
4. Phase 4 — Iterative Sub-Agent-Reviews mit rotierenden Fokus-Vektoren (Pass-Anzahl: klein, ~3–5 Tasks → 2 Pässe)

Abarbeitung via `superpowers:subagent-driven-development` mit Pre-/Post-KPI-Snapshots (ADR-0021).
