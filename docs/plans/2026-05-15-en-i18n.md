# EN-i18n Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use `superpowers:subagent-driven-development` (recommended) or `superpowers:executing-plans` to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Status:** v3 (post-subagent-5-restrisiko, ready for implementation)

**Iteration-Log:**

- v2 → v3: Plan-Pass-5 (Restrisiko + Pro-Task-Konfidenz) — keine neuen AUTO-FIXes. Pro-Task-Konfidenz Phase 1+2+4 `high`, Phase 3 `medium-high`. Größtes Risiko: Type-Cascade-Transient zwischen Commit 2 und 3 (explizit in Commit-Message dokumentiert, durch Commit-Bundelung minimiert). Rollback-Pfad via `git revert` pro Commit sauber. Task 3.3 Discovery-Wortlaut präzisiert (Snippet ist authoritativ, kein offener Entscheidungspunkt).
- v1 → v2: Plan-Pass-1 (Spec-Plan-Coverage) — vollständig, ein Mini-Gap (§5.1 YAML-Edge-Case) in Notizen ergänzt. Plan-Pass-2 (Konsistenz + Architektur + Conventions §1–§11): 4 AUTO-FIX (Task 3.3 `nodeName` präziser via `id === 'g_unassigned'`-Check, Task 3.5 Step 5 RenderContext-Felder-Liste vollständig, Task 3.7 Step 2 WHY-Kommentar, Task 4.5 Step 4+5 `editor.ts` LOC-Limit 400 → 410 in conventions.md UND scripts/kpi.mjs synchron) + 2 USER-DECISION (P2-U1: alle 3 Editor-Render-Funktionen auf Argument-Objekt-Pattern, conventions §1.5 konform; P2-U2: editor.ts LOC-Limit 410 formalisiert).

**Goal:** Englische Sprachunterstützung über `hass.locale.language` mit `resolveT(lang)`-Factory; `de*` → DE, alles andere → EN. Kein Config-Eingriff, automatische Sprach-Auswahl.

**Architecture:** Zwei neue Files in `src/i18n/` (`index.ts` mit `Widen<T>`-Mapped-Type, `Lang`, `langFromHass`, `resolveT`; `en.ts` mit vollständiger EN-Übersetzung typisiert gegen `Widen<typeof DE>`). `RenderContext` bekommt `t: Translations`-Feld; `card.ts` setzt `_lang` in `willUpdate`, `editor.ts` bekommt ein NEUES `willUpdate`. Engine, util, ha, config-Schema bleiben unangetastet. ESLint `no-restricted-paths` erzwingt: `i18n/` darf nichts außer `i18n/` importieren.

**Tech Stack:** TypeScript 5.4 strict (`noUncheckedIndexedAccess`, `noPropertyAccessFromIndexSignature`), Lit 3, Vitest, ESLint, Rollup, pnpm. Test-Env Vitest mit happy-dom für `editor*.test.ts`/`card.test.ts`. Bundle ≤ 64 KiB (ADR-0022, erwartet ~61 KiB nach Refactor), `card.ts` ≤ 200 LOC (heute 178), `editor.ts` heute 405 LOC (Conv-Tech-Debt — darf nicht über 410 wachsen).

**Verbindliche Lese-Quellen (vor Start):**

- Spec: `docs/specs/2026-05-15-en-i18n.md` (v7) — **Single-Source aller Constraints, Werte, Begründungen**
- `CLAUDE.md` — Projekt-Schnellreferenz, Regeln 1-10, Workflow
- `docs/conventions.md` — Code-Stil §1.5/§3/§8/§11/§12/§13/§15
- `docs/architecture.md` — Layer-Tabelle §2, ADR-Tabelle §4
- ADR-0009 (ESLint Layer Boundaries), ADR-0010 (Single-Source), ADR-0011 (shouldUpdate), ADR-0022 (Bundle-Budget)

**Konzepte (verbindlich, siehe Spec für Details):**

- **Datenfluss-Pipeline** (Spec §0.3): `langFromHass(hass) → Lang → resolveT(lang) → Translations`. Wird in `card.ts`/`editor.ts` aufgerufen, an `render/`-Module via `RenderContext.t` durchgereicht, an `battery-ring`/`card-helpers`/`editor-list-sections` als Parameter.
- **Lit-Lifecycle** (Spec §0.4): `_lang` wird in `willUpdate` gesetzt, `ctx.t` wird in `render` aufgebaut (PURE). `shouldUpdate` erkennt Locale-Wechsel.
- **Code-Reuse-Tabelle** (Spec §2.4): `Translations`, `Lang`, `langFromHass`, `resolveT`, `RenderContext.t` — alle anderen Helper (`colorFor`, `formatPowerW`, `fireMoreInfo`) sind sprachneutral und brauchen kein i18n.
- **Anti-Patterns** (Spec §2.4): kein Modul-Singleton, keine eigene `Lang`-Type, kein `as Translations`-Cast, kein ad-hoc-Locale-Vergleich in Renderer.

**Standing Requirement für jeden Task** (Disziplin-Block, gilt durchgehend):

> 🛑 Jede Code-Zeile, die in dieser Plan-Datei als TypeScript-Block steht, ist als Vorschlag zu verstehen, der **`docs/conventions.md`** und **alle in Spec §0.1 referenzierten ADRs** einhalten muss. Vor dem `git commit` jedes Tasks: **`pnpm check` läuft grün durch** (`lint + typecheck + test`). Lint erzwingt automatisch:
>
> - Layer-Boundaries (ADR-0009) — `i18n/` darf nichts außer `i18n/` importieren
> - Imports-Reihenfolge (`import/order`, conventions §4): external → internal → type-only, alphabetisch innerhalb der Gruppe
> - `no-console` außer info/warn/error (conventions §7)
> - `no-explicit-any` ohne expliziten Disable
> - `no-non-null-assertion` außer in Tests
>
> Entwickler-Disziplin erzwungen manuell:
>
> - Pure Functions in `engine/` (ADR-0004) — Engine wird in diesem Plan NICHT angefasst
> - Keine god-class in `card.ts` (≤ 200 LOC) — heute 178, Wachstum ≤ 22 LOC
> - Single-Source `util/`-Aufrufe (ADR-0010) — `CARD_NAME` aus `const.ts:5` bleibt, `EN.card.name` muss konsistent sein
> - Funktionale Iteration (`.map`/`.filter`/`.reduce` über `forEach + push`, conventions §1.6); Walker in `en.test.ts` ist `for…of` für Side-Effects (Test-Assertions) — erlaubt
> - Conventional-Commit-Format (conventions §8): scopes aus `engine, render, config, editor, util, ha, card, i18n, docs, build, ci`
> - Strings aus `i18n/`, niemals hardcoded
> - **TDD-First** für `i18n/`: Tests rot, dann Code, dann grün
>
> **KPI-Snapshot-Pflicht (CLAUDE.md „Code-Review — Workflow"):**
>
> - Vor erstem Task: `pnpm test:coverage && pnpm build && pnpm kpi:snapshot --label pre-en-i18n --phase pre`
> - Nach letztem Task: `pnpm test:coverage && pnpm build && pnpm kpi:snapshot --label post-en-i18n --phase post`
> - Code-Review-Workflow läuft danach VOR `finishing-a-development-branch`

**Elements NICHT anfassen** (Spec §0.0 TL;DR + §0.5 Don't-Touch — 1:1 hier reproduziert):

| Element                                               | Wo                                           | Warum                                                                                                                             |
| ----------------------------------------------------- | -------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------- |
| `src/engine/*`                                        | komplett                                     | Pure Energie-Bilanz, kennt keine Strings (CLAUDE.md 1, ADR-0004)                                                                  |
| `src/util/*`                                          | komplett (außer `const.ts` Version-Bump)     | Layer-Zone darf nichts importieren (ADR-0009)                                                                                     |
| `src/ha/*`                                            | komplett                                     | HA-Boundary-Types, sprachneutral                                                                                                  |
| `DE`-Konstante                                        | `src/i18n/de.ts`                             | Source-of-Truth für `Widen<typeof DE>`, Struktur unverändert                                                                      |
| Config-Schema                                         | `src/config/schema.ts`                       | Kein neues Feld; nur Type-Anpassung in `types.ts`                                                                                 |
| `aria-label` strukturell                              | `node-renderer.ts:68`, `flow-renderer.ts:38` | Werte werden lokalisiert, Struktur bleibt                                                                                         |
| `::part()`-Hooks                                      | überall                                      | Theme-Hooks sprachunabhängig                                                                                                      |
| Animation, Layout, Memo                               | `flow-animation.ts`, `layout.ts`, `memo.ts`  | Sprachneutral                                                                                                                     |
| `hacs.json`                                           | komplett                                     | Keine Versionsangabe, keine Strings (verifiziert)                                                                                 |
| `examples/2-pv-2-batt.yaml`                           | komplett                                     | YAML-User-Overrides (User-Daten, bewusst akzeptiert)                                                                              |
| `derive-display-consumers.ts:43` (mapNoneMode)        | nur Line 43                                  | i18n-Import bleibt — Default-Consumer-Naming „Verbraucher N" lokalisiert zur Config-Auswertungs-Zeit (akzeptierter v1.x-Refactor) |
| Editor-Stub-Default `'Verbraucher'`                   | `editor.ts:364`                              | YAML-Initialwert, User-überschreibbar, kein Render-Zeit-i18n                                                                      |
| `customElements`/`@customElement`-Decorator-Verhalten | `card.ts:22`, `editor.ts:21`                 | Lit-Reaktivität via `@property`/`@state`, keine `hasChanged`-Override                                                             |

**Phases:**

- **Phase 0.** KPI-Pre-Snapshot (verbindlich, vor erstem Task)
- **Phase 1.** i18n-Infrastruktur + Tests (Commit 1, 7 Tasks)
- **Phase 2.** Config-Type-Change (Commit 2, 4 Tasks)
- **Phase 3.** Caller-Refactor (Commit 3, 14 Tasks)
- **Phase 4.** Doku + Release-Bump (Commit 4, 11 Tasks)
- **Phase 5.** KPI-Post-Snapshot + Code-Review-Workflow (verbindlich, vor `finishing-a-development-branch`)

---

## File Structure (decomposed before tasks)

### Modified

| Datei                                                     | Verantwortlichkeit                                                                                                                                                                      | Phase |
| --------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----- |
| `vitest.config.ts`                                        | `coverage.include` um `'src/i18n/**'` erweitern                                                                                                                                         | 1     |
| `scripts/kpi.mjs`                                         | `COVERAGE_REQUIRED_LAYERS` um `'i18n'` erweitern                                                                                                                                        | 1     |
| `src/config/types.ts`                                     | `DisplayConsumer.name: string` → `string \| undefined` (Line 82)                                                                                                                        | 2     |
| `src/config/derive-display-consumers.ts`                  | Line 72 (`unassignedGroup`): `name: undefined`; Line 43 (`mapNoneMode`) bleibt mit `DE`                                                                                                 | 2     |
| `src/config/derive-display-consumers.test.ts`             | Test für unassigned-Gruppe: `expect(consumer.name).toBeUndefined()`                                                                                                                     | 2     |
| `src/render/context.ts`                                   | `RenderContext` bekommt `t: Translations`-Feld                                                                                                                                          | 3     |
| `src/render/flow-renderer.ts`                             | `DE`-Import entfernen; `ctx.t.card.name`, `ctx.t.diagnostics.*`                                                                                                                         | 3     |
| `src/render/node-renderer.ts`                             | `DE`-Import entfernen; `ctx.t.states.sensorUnavailable`, `ctx.t.nodes.*` (in `nodeName`); `node.name ?? ctx.t.nodes.unassignedGroup`                                                    | 3     |
| `src/render/battery-ring.ts`                              | `DE`-Import entfernen; `t: Translations` als Parameter zu `formatSocPct`+`renderBatteryRing`                                                                                            | 3     |
| `src/render/battery-ring.test.ts`                         | Renderer-Aufrufe mit `T = DE` als Parameter; Test Line 134 (Late-Import von `DE`) bleibt                                                                                                | 3     |
| `src/card.ts`                                             | `DE`-Import entfernen; `@state _lang: Lang`; `willUpdate` setzt `_lang`; `shouldUpdate` Locale-Vergleich; `render` baut `ctx.t = resolveT(this._lang)`; 3 inline-Strings auf `T.*`      | 3     |
| `src/card-helpers.ts`                                     | `DE`-Import entfernen; `renderSkeleton(t: Translations)` mit Parameter                                                                                                                  | 3     |
| `src/card-helpers.test.ts`                                | Helper-Tests passen Signaturen an; +Sprach-Branching-Test mit `it.each(['de','en'])`                                                                                                    | 3     |
| `src/editor.ts`                                           | `DE`-Import entfernen; `@state _lang: Lang`; NEUES `willUpdate(changed)` für `_lang`-Set; `render()` ruft `resolveT(this._lang)`, reicht `T` an Sektionen + General/Grid-Render-Helpers | 3     |
| `src/editor-list-sections.ts`                             | `DE`-Import entfernen; `t: Translations` als zusätzlicher Parameter zu allen drei Render-Funktionen                                                                                     | 3     |
| `src/editor-list-sections.test.ts`                        | Tests passen Signaturen an; +Sprach-Branching-Test mit `it.each(['de','en'])`                                                                                                           | 3     |
| `src/index.ts`                                            | `DE`-Import (Line 4) entfernen; `EN` aus `./i18n/en` importieren; Line 33 `description: EN.card.description`                                                                            | 3     |
| `examples/preview-mocks.ts`                               | `buildMockHass()` (Line 451) bekommt `locale: { language: 'de' \| 'en' }`-Feld, Default `'de'`; optional Parameter `lang` für Test-Szenarien                                            | 3     |
| `examples/preview.html`                                   | UI-Toggle: zwei Buttons „DE" / „EN", click-Handler ändert `hass.locale.language` und re-rendert Card                                                                                    | 3     |
| `src/const.ts`                                            | `CARD_VERSION: '0.13.0'` → `'0.14.0'` (Line 3)                                                                                                                                          | 4     |
| `package.json`                                            | `version: '0.13.0'` → `'0.14.0'`                                                                                                                                                        | 4     |
| `docs/specs/2026-05-10-custom-energy-flow-card-design.md` | §1.3 In-Scope-Bullet ergänzen, §1.4 Out-of-Scope-Bullet streichen/markieren                                                                                                             | 4     |
| `CLAUDE.md`                                               | Kritische Regel 8 sprachneutral formulieren                                                                                                                                             | 4     |
| `docs/conventions.md`                                     | §11 Anti-Pattern-Bullet anpassen, §15 Sprache erweitern, neuer Pflege-Hinweis                                                                                                           | 4     |
| `docs/architecture.md`                                    | §2 Modulkarte `i18n/`-Beschreibung erweitern, §4 ADR-0023 hinzufügen                                                                                                                    | 4     |
| `docs/adr/README.md`                                      | ADR-0023 in Index eintragen                                                                                                                                                             | 4     |
| `README.md`                                               | Changelog-Eintrag 0.14.0 (Englische Sprachunterstützung + Breaking Visual Change)                                                                                                       | 4     |

### Created

| Datei                                   | Verantwortlichkeit                                                                                         | Phase |
| --------------------------------------- | ---------------------------------------------------------------------------------------------------------- | ----- |
| `src/i18n/index.ts`                     | `type Translations = Widen<typeof DE>`, `type Lang = 'de' \| 'en'`, `langFromHass(hass)`, `resolveT(lang)` | 1     |
| `src/i18n/index.test.ts`                | Locale-Match-Tests + `resolveT`-Identitäts-Tests + `CARD_NAME ↔ DE.card.name ↔ EN.card.name`-Konsistenz    | 1     |
| `src/i18n/en.ts`                        | `export const EN: Translations = {...}` — vollständige EN-Übersetzung                                      | 1     |
| `src/i18n/en.test.ts`                   | Struktur-Walker (jeder DE-Pfad in EN, gleicher Typ) + funktionale Strings (`pluralize`, `pairingMissing`)  | 1     |
| `docs/adr/0023-i18n-via-hass-locale.md` | Neuer ADR (Stub aus Spec §4.1)                                                                             | 4     |

### NICHT anfassen (Spec §0.2 NICHT-berührte Layer)

- `src/engine/*` — pure Energie-Bilanz, kein i18n-Bezug (ADR-0004)
- `src/util/*` (außer `const.ts` Version-Bump) — Single-Source-Helfer, sprachneutral
- `src/ha/*` — HA-Boundary-Types, sprachneutral
- `src/i18n/de.ts` — Source-of-Truth für `Widen<typeof DE>`, Struktur bleibt 1:1
- `src/config/schema.ts`, `src/config/system-state.ts` — kein neues Schema-Feld
- `src/render/{layout.ts,layout.test.ts,home-ring.ts,icon.ts,icon.test.ts,flow-animation.ts,edge-color.ts,theme.ts}` — sprachneutral, kein `DE`-Import
- `src/engine/*.test.ts`, `src/util/*.test.ts`, `src/config/{schema,system-state}.test.ts` — sprachneutral
- `tests/setup/ha-icon.ts` — Test-Setup, kein i18n-Bezug
- `scripts/smoke-test.mjs` — Smoke-Test rendert mit Mock-hass ohne `locale` (fällt auf EN), kein Edit nötig — siehe Spec §5.4 Convention
- `hacs.json` — keine Versionsangabe, sprachunabhängig (verifiziert)
- `examples/2-pv-2-batt.yaml`, `examples/with-grouping.yaml` — User-Override-Demonstrationen, strukturell unverändert
- `scripts/build-preview.mjs` — Build-Script ohne i18n-Bezug

### Build-Pipeline-Files (wichtig — oft übersehen)

| Datei                       | Art der Änderung                                                                                                                                                 | Phase |
| --------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----- |
| `.eslintrc.cjs`             | **KEINE** — Layer-Zone für `i18n/` ist heute schon korrekt (`except: ['./i18n']`); editor.ts/editor-list-sections.ts haben i18n bereits in ihren `except`-Listen | —     |
| `vitest.config.ts`          | Line 14 `coverage.include` erweitern um `'src/i18n/**'`                                                                                                          | 1     |
| `tsconfig.json`             | **KEINE** — Test-Files sind via `exclude: ["**/*.test.ts"]` aus tsc raus (bewusster Tech-Debt, siehe Spec §0.2.1)                                                | —     |
| `package.json`              | `version: 0.13.0 → 0.14.0`; KEINE neuen Deps                                                                                                                     | 4     |
| `scripts/build-preview.mjs` | **KEINE** — kein i18n-Bezug                                                                                                                                      | —     |
| `scripts/smoke-test.mjs`    | **KEINE** — keine sprachabhängigen Assertions, Mock-hass ohne `locale` ist OK (Spec §5.4)                                                                        | —     |
| `scripts/kpi.mjs`           | Line 30 `COVERAGE_REQUIRED_LAYERS` erweitern: `['engine', 'config', 'util', 'i18n']`                                                                             | 1     |

---

## Phase 0 — KPI-Pre-Snapshot (verbindlich, vor erstem Task)

### Task 0.1: Pre-Snapshot erfassen

**Files:** none modified

- [ ] **Step 1: Sicherstellen, dass `main` grün ist**

```bash
pnpm check
```

Erwartet: `lint + typecheck + test` alle grün.

- [ ] **Step 2: Build + Coverage erzeugen (Voraussetzung für KPI-Snapshot)**

```bash
pnpm build && pnpm test:coverage
```

Erwartet: Bundle in `dist/custom-energy-flow-card.js`, `coverage-summary.json` in `coverage/`.

- [ ] **Step 3: KPI-Pre-Snapshot speichern**

```bash
pnpm kpi:snapshot --label pre-en-i18n --phase pre
```

Erwartet: ein neuer Eintrag in `metrics/kpi-history.json` mit `phase: "pre"`, `label: "pre-en-i18n"`.

- [ ] **Step 4: Baseline-Werte notieren (für Code-Review-Pass-3-Delta)**

Bundle-Baseline aus `metrics/kpi-history.json` (letzter `bundle_bytes`): **~61475 Bytes (60.0 KiB)**. Erwartet nach Implementation: ~62500 Bytes (61 KiB), klar unter 65536 Bytes (64 KiB) Budget.

`card.ts` LOC: 178. Wachstum ≤ 22 LOC bis zum Hard-Limit 200.
`editor.ts` LOC: 405. Wachstum ≤ 5 LOC bis Spec-Toleranz 410 (Tech-Debt-Limit).

---

## Phase 1 — i18n-Infrastruktur + Tests (Commit 1)

**Commit-Vorlage:**

```
feat(i18n): add resolveT factory + EN translations

Zwei neue Files in src/i18n/:
- index.ts: Widen<T>-Mapped-Type (umgeht as-const-Literal in de.ts),
  Lang-Type, langFromHass(hass), resolveT(lang).
- en.ts: vollständige EN-Übersetzung gegen Widen<typeof DE> typisiert.

vitest.config.ts: coverage.include += 'src/i18n/**'.
scripts/kpi.mjs: COVERAGE_REQUIRED_LAYERS += 'i18n' (Hard-Gate).

Subspec 2026-05-15-en-i18n (v7) §1.1, §1.2, §3.1, §3.2.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
```

### Task 1.1: Tests rot — `i18n/index.test.ts`

**Files:**

- Create: `src/i18n/index.test.ts`

- [ ] **Step 1: Test-File anlegen, alle Test-Cases schreiben**

```ts
import { describe, expect, it } from 'vitest';
import { CARD_NAME } from '../const';
import { DE } from './de';
import { EN } from './en';
import { langFromHass, resolveT } from './index';

describe('langFromHass', () => {
  it.each([
    ['de', 'de'],
    ['de-DE', 'de'],
    ['de-AT', 'de'],
    ['de-CH', 'de'],
    ['DE', 'de'], // case-insensitive defensive
    ['en', 'en'],
    ['en-US', 'en'],
    ['en-GB', 'en'],
    ['fr', 'en'],
    ['es', 'en'],
    ['ja', 'en'],
    ['', 'en'],
  ] as const)('hass.locale.language=%s → %s', (lang, expected) => {
    expect(langFromHass({ locale: { language: lang } })).toBe(expected);
  });

  it('undefined hass → en', () => {
    expect(langFromHass(undefined)).toBe('en');
  });

  it('hass without locale → en', () => {
    expect(langFromHass({})).toBe('en');
  });

  it('hass.locale without language → en', () => {
    expect(langFromHass({ locale: {} })).toBe('en');
  });
});

describe('resolveT', () => {
  it('resolveT(de) === DE (referential identity)', () => {
    expect(resolveT('de')).toBe(DE);
  });
  it('resolveT(en) === EN (referential identity)', () => {
    expect(resolveT('en')).toBe(EN);
  });
});

// CARD_NAME ↔ DE.card.name ↔ EN.card.name Single-Source-Konsistenz (ADR-0010)
describe('CARD_NAME Konsistenz', () => {
  it('CARD_NAME === DE.card.name === EN.card.name', () => {
    expect(CARD_NAME).toBe(DE.card.name);
    expect(CARD_NAME).toBe(EN.card.name);
  });
});
```

- [ ] **Step 2: Tests ausführen — MUSS rot sein**

```bash
pnpm test src/i18n/index.test.ts
```

Erwartet: alle Tests sind **rot** (`./en` + `./index` existieren nicht). Sanity-Check: wenn ein Test grün bleibt, der eigentlich rot sein sollte — Test-Bypass-Verdacht. **STOP**, melden, nicht weiter.

- [ ] **Step 3: KEIN Commit jetzt (Rot-State)**

### Task 1.2: Tests rot — `i18n/en.test.ts`

**Files:**

- Create: `src/i18n/en.test.ts`

- [ ] **Step 1: Test-File anlegen mit Walker + funktionalen Tests**

```ts
import { describe, expect, it } from 'vitest';
import { DE } from './de';
import { EN } from './en';

/** Rekursiver Walker: jeder Pfad in DE existiert in EN mit gleichem Typ. */
function walk(a: unknown, b: unknown, path = ''): void {
  if (typeof a === 'function') {
    expect(typeof b).toBe('function');
    return;
  }
  if (typeof a === 'string') {
    expect(typeof b).toBe('string');
    expect((b as string).length).toBeGreaterThan(0);
    return;
  }
  if (a && typeof a === 'object') {
    expect(b).toBeTypeOf('object');
    expect(b).not.toBeNull();
    // for...of mit Side-Effects (expect-Assertions) ist via conventions §1.6 erlaubt
    for (const key of Object.keys(a)) {
      expect(b).toHaveProperty(key);
      // Internal-Cast (conventions §1.2): walk descends only into Object-Branches,
      // toHaveProperty oben garantiert Key-Existenz vor Index-Zugriff
      walk(
        (a as Record<string, unknown>)[key],
        (b as Record<string, unknown>)[key],
        `${path}.${key}`,
      );
    }
  }
}

describe('EN strukturell identisch zu DE', () => {
  it('jeder DE-Pfad existiert in EN mit gleichem Typ', () => {
    walk(DE, EN);
  });
});

describe('EN funktionale Strings', () => {
  it('pluralize: 0 → warnings, 1 → warning, 2 → warnings', () => {
    expect(EN.diagnostics.pluralize(0)).toBe('warnings');
    expect(EN.diagnostics.pluralize(1)).toBe('warning');
    expect(EN.diagnostics.pluralize(2)).toBe('warnings');
  });
  it('pairingMissing enthält Argument-ID und ist englisch', () => {
    const msg = EN.editor.pairingMissing('foo-1');
    expect(msg).toContain('foo-1');
    expect(msg).toMatch(/does not exist|missing/i);
  });
});
```

- [ ] **Step 2: Tests ausführen — MUSS rot sein**

```bash
pnpm test src/i18n/en.test.ts
```

Erwartet: rot, da `./en` nicht existiert. **STOP** wenn grün.

- [ ] **Step 3: KEIN Commit jetzt (Rot-State)**

### Task 1.3: Code — `i18n/index.ts` mit Widen-Type-Helper

**Files:**

- Create: `src/i18n/index.ts`

- [ ] **Step 1: Resolver-Modul schreiben**

```ts
import { DE } from './de';
import { EN } from './en';

/**
 * Mapped-Type-Widening:
 * `src/i18n/de.ts` endet mit `as const` → `typeof DE` ist Literal-Type (z.B.
 * `editor.sectionBattery: 'Akkus'`). `EN: typeof DE` mit Wert `'Batteries'`
 * würde TypeError werfen (`'Batteries' is not assignable to '"Akkus"'`).
 *
 * Widen<T> ersetzt string-Literale durch `string`, Funktionen bleiben unverändert,
 * Objekte rekursiv. Damit ist Translations strukturell kompatibel zu typeof DE
 * UND erlaubt jeden anderen string in EN.
 */
type Widen<T> = {
  readonly [K in keyof T]: T[K] extends string
    ? string
    : T[K] extends (...args: infer A) => infer R
      ? (...args: A) => R
      : Widen<T[K]>;
};

export type Translations = Widen<typeof DE>;
export type Lang = 'de' | 'en';

/**
 * Strukturelle hass-Sicht — kein HA-Type-Import, damit i18n-Layer-Zone gewahrt bleibt.
 * Hinweis: src/ha/ha-types.ts:14 hat `locale?: { language: string }` (language non-optional).
 * Wir halten `language` hier OPTIONAL als defensive Verteidigung gegen partielle hass-Objekte
 * vor dem ersten setHass-Update.
 */
type HassLocaleView = { locale?: { language?: string } } | undefined;

export function langFromHass(hass: HassLocaleView): Lang {
  const lang = hass?.locale?.language;
  if (typeof lang === 'string' && lang.toLowerCase().startsWith('de')) return 'de';
  return 'en';
}

export function resolveT(lang: Lang): Translations {
  return lang === 'de' ? DE : EN;
}
```

- [ ] **Step 2: typecheck — muss grün sein, aber Tests bleiben rot bis Task 1.4**

```bash
pnpm typecheck
```

Erwartet: grün. Tests für `index.ts` (Task 1.1) sind noch rot, weil `./en` nicht existiert.

### Task 1.4: Code — `i18n/en.ts` mit vollständiger EN-Übersetzung

**Files:**

- Create: `src/i18n/en.ts`

- [ ] **Step 1: EN-Konstante schreiben (1:1-Struktur zu DE, alle Strings englisch)**

```ts
import type { Translations } from './index';

export const EN: Translations = {
  card: {
    name: 'Custom Energy Flow Card',
    description:
      'Multi-source energy-flow visualization with any number of PVs, batteries and consumers',
  },
  nodes: {
    solar: 'Solar',
    battery: 'Battery',
    grid: 'Grid',
    home: 'Home',
    consumer: 'Consumer',
    unassignedGroup: 'Other',
  },
  units: { watt: 'W', percent: '%' },
  states: {
    loading: 'Loading …',
    sensorUnavailable: 'Sensor unavailable',
    stubHint: 'Add solar, battery or consumer entries to see the energy diagram.',
    cardError: 'Card error',
    narrowBanner: 'Best viewed at 320 px width or more',
  },
  grid: { consumption: 'Import', feedIn: 'Export' },
  battery: { charging: 'charging', discharging: 'discharging' },
  diagnostics: {
    iconLabel: 'Diagnostics',
    title: 'Engine warnings',
    pluralize: (n: number): string => (n === 1 ? 'warning' : 'warnings'),
  },
  editor: {
    sectionGeneral: 'General',
    sectionSolar: 'Solar systems',
    sectionBattery: 'Batteries',
    sectionGrid: 'Grid',
    sectionConsumers: 'Consumers',
    sectionDisplay: 'Display',
    addSolar: '+ Add PV',
    addBattery: '+ Add battery',
    addConsumer: '+ Add consumer',
    chargedBy: 'Charged by',
    chargedByPlaceholder: '— select —',
    pairingMissing: (id: string) => `PV ID "${id}" does not exist`,
    moveUp: '↑',
    moveDown: '↓',
    remove: 'Remove',
    consumerGroupingLabel: 'Consumer grouping',
    consumerGroupingNone: 'None',
    consumerGroupingByArea: 'By HA area',
  },
};
```

- [ ] **Step 2: Tests ausführen — i18n-Tests müssen jetzt grün**

```bash
pnpm test src/i18n/
```

Erwartet: `index.test.ts` + `en.test.ts` beide grün. Falls Walker-Test rot ist (fehlender Key): EN ergänzen, nicht Walker abschwächen.

- [ ] **Step 3: typecheck — muss grün sein**

```bash
pnpm typecheck
```

Erwartet: grün. Wenn `EN: Translations` rote Type-Errors gibt (Literal-Drift): `Widen<T>`-Logik in `index.ts` prüfen. **STOP** wenn Type-Errors trotz Widen.

### Task 1.5: Build-Pipeline — `vitest.config.ts` coverage.include erweitern

**Files:**

- Modify: `vitest.config.ts`

- [ ] **Step 1: `coverage.include` Array erweitern**

```ts
// alt (Line 14)
include: ['src/engine/**', 'src/config/**', 'src/util/**'],

// neu
include: ['src/engine/**', 'src/config/**', 'src/util/**', 'src/i18n/**'],
```

- [ ] **Step 2: Coverage-Lauf — `i18n/` muss im Report auftauchen**

```bash
pnpm test:coverage
```

Erwartet: `i18n/index.ts` und `i18n/en.ts` und `i18n/de.ts` im Coverage-Report mit ≥ 90 % (Tests aus Tasks 1.1+1.2 decken vollständig ab).

### Task 1.6: Build-Pipeline — `scripts/kpi.mjs` Layer-Liste

**Files:**

- Modify: `scripts/kpi.mjs`

- [ ] **Step 1: `COVERAGE_REQUIRED_LAYERS` erweitern**

```js
// alt (Line 30)
const COVERAGE_REQUIRED_LAYERS = ['engine', 'config', 'util'];

// neu
const COVERAGE_REQUIRED_LAYERS = ['engine', 'config', 'util', 'i18n'];
```

- [ ] **Step 2: KPI-Lauf — `i18n` muss im Layer-Report auftauchen**

```bash
pnpm kpi
```

Erwartet: JSON-Output enthält `layers.i18n.coverage_pct` mit numerischem Wert (≥ 90 %). Falls null: `coverage-summary.json` fehlt — `pnpm test:coverage` zuerst.

### Task 1.7: Verifikation + Commit 1

- [ ] **Step 1: `pnpm check` grün**

```bash
pnpm check
```

- [ ] **Step 2: Commit erstellen**

```bash
git add src/i18n/index.ts src/i18n/index.test.ts src/i18n/en.ts src/i18n/en.test.ts \
        vitest.config.ts scripts/kpi.mjs
git commit -m "$(cat <<'EOF'
feat(i18n): add resolveT factory + EN translations

Zwei neue Files in src/i18n/:
- index.ts: Widen<T>-Mapped-Type (umgeht as-const-Literal in de.ts),
  Lang-Type, langFromHass(hass), resolveT(lang).
- en.ts: vollständige EN-Übersetzung gegen Widen<typeof DE> typisiert.

vitest.config.ts: coverage.include += 'src/i18n/**'.
scripts/kpi.mjs: COVERAGE_REQUIRED_LAYERS += 'i18n' (Hard-Gate).

Subspec 2026-05-15-en-i18n (v7) §1.1, §1.2, §3.1, §3.2.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

- [ ] **Step 3: `pnpm check` nach Commit nochmal grün**

**STOP-Condition:** Wenn `pnpm check` zwischen Task 1.4 und 1.7 nicht grün ist — Phase nicht abschließen. Code-Review mit User abklären.

---

## Phase 2 — Config-Type-Change (Commit 2)

**Commit-Vorlage:**

```
refactor(config): make DisplayConsumer.name optional + unassigned-group fix

DisplayConsumer.name: string → string | undefined.
derive-display-consumers.ts:72 (groupByArea-unassigned): name = undefined
(Renderer löst gegen ctx.t.nodes.unassignedGroup auf).
Line 43 (mapNoneMode default naming) bleibt mit DE-Import — siehe
Subspec 2026-05-15-en-i18n (v7) §2.2 Out-of-Scope-Hinweis.

Tests umgestellt: expect(consumer.name).toBeUndefined() für unassigned.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
```

### Task 2.1: Tests anpassen — `derive-display-consumers.test.ts`

**Files:**

- Modify: `src/config/derive-display-consumers.test.ts`

- [ ] **Step 1: Test für unassigned-Gruppe umstellen**

Suche im Test-File die Stelle, die `name: 'Sonstige'` (`DE.nodes.unassignedGroup`) für die unassigned-Gruppe erwartet. Heute typischerweise:

```ts
// alt (Beispiel — exakte Zeile per grep finden):
expect(unassignedGroup.name).toBe('Sonstige');
// oder:
expect(consumer).toMatchObject({ id: 'g_unassigned', name: 'Sonstige' });
```

```ts
// neu:
expect(unassignedGroup.name).toBeUndefined();
// oder:
expect(consumer).toMatchObject({ id: 'g_unassigned', name: undefined });
```

- [ ] **Step 2: Test ausführen — MUSS rot sein**

```bash
pnpm test src/config/derive-display-consumers.test.ts
```

Erwartet: rot. **STOP** wenn grün.

- [ ] **Step 3: KEIN Commit jetzt**

### Task 2.2: Code — `config/types.ts` Type-Anpassung

**Files:**

- Modify: `src/config/types.ts`

- [ ] **Step 1: `DisplayConsumer.name` optional machen**

```ts
// alt (Line 82):
name: string;
// Anzeige-Name (von Area oder vom einzelnen consumer).

// neu:
name?: string;
// undefined ⇔ unassigned-group (Renderer löst gegen ctx.t.nodes.unassignedGroup auf, siehe Subspec §2.2)
```

- [ ] **Step 2: typecheck — Compiler zeigt jetzt alle Stellen, die `name`-non-null-Annahme verletzen**

```bash
pnpm typecheck
```

Erwartet: möglicherweise neue Errors in `node-renderer.ts:nodeName()` und `derive-display-consumers.ts` (wo `name` als `string` weiterverarbeitet wird). Diese werden in Tasks 2.3 + 3.3 gefixt.

### Task 2.3: Code — `derive-display-consumers.ts:72` unassigned undefined

**Files:**

- Modify: `src/config/derive-display-consumers.ts`

- [ ] **Step 1: Line 72 anpassen**

```ts
// alt (Line 72):
{
  id: 'g_unassigned',
  name: DE.nodes.unassignedGroup,
  members: ...,
  areaId: undefined,
}

// neu:
{
  id: 'g_unassigned',
  name: undefined,
  members: ...,
  areaId: undefined,
}
```

**WICHTIG:** Line 43 (`mapNoneMode`) bleibt unverändert mit `c.name || \`${DE.nodes.consumer} ${i + 1}\``— siehe Spec §2.2 Out-of-Scope-Hinweis.`DE`-Import auf Line 1 bleibt deshalb erhalten.

- [ ] **Step 2: Test ausführen — MUSS grün sein**

```bash
pnpm test src/config/derive-display-consumers.test.ts
```

- [ ] **Step 3: typecheck — Renderer-Errors auf `name`-Zugriff bleiben (werden in Phase 3 gefixt)**

```bash
pnpm typecheck
```

Erwartet: TypeScript meldet jetzt Errors in `node-renderer.ts` (nodeName-Funktion liest `entry.name` ohne null-Check). Das ist OK — Phase 3 wird den `ctx.t.nodes.unassignedGroup`-Fallback einführen. **Aktuell `pnpm check` ist NICHT grün** zwischen Phase 2 und Phase 3.

### Task 2.4: Verifikation + Commit 2

**Schwierigkeit:** Zwischen Phase 2 und Phase 3 ist `pnpm typecheck` ERWARTUNGSGEMÄSS rot, weil Renderer noch nicht refaktorisiert ist. Wir committen den Type-Change trotzdem, weil Commit 3 unmittelbar folgt und den Fix bringt.

- [ ] **Step 1: `pnpm test` allein grün (Tests die im Type-Change angepasst wurden)**

```bash
pnpm test src/config/
```

Erwartet: alle config-Tests grün.

- [ ] **Step 2: Lint grün**

```bash
pnpm lint
```

- [ ] **Step 3: Commit erstellen — typecheck bleibt rot bis Phase 3 abgeschlossen ist**

```bash
git add src/config/types.ts src/config/derive-display-consumers.ts src/config/derive-display-consumers.test.ts
git commit -m "$(cat <<'EOF'
refactor(config): make DisplayConsumer.name optional + unassigned-group fix

DisplayConsumer.name: string → string | undefined.
derive-display-consumers.ts:72 (groupByArea-unassigned): name = undefined
(Renderer löst gegen ctx.t.nodes.unassignedGroup auf in Phase 3).
Line 43 (mapNoneMode default naming) bleibt mit DE-Import — siehe
Subspec 2026-05-15-en-i18n (v7) §2.2 Out-of-Scope-Hinweis.

HINWEIS: `pnpm typecheck` ist zwischen Commit 2 und Commit 3 rot
(node-renderer.ts:nodeName erwartet noch `entry.name` als string).
Wird in Commit 3 (Caller-Refactor) gefixt.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

**STOP-Condition:** wenn `pnpm typecheck` nach Commit 2 unerwartet grün ist → der Type-Change hat nicht alle erwarteten Stellen aktiviert. Manual review der `nodeName`-Funktion in `node-renderer.ts:213-225` (`entry.name` muss `string | undefined` sein). Korrigieren oder Plan re-evaluieren.

---

## Phase 3 — Caller-Refactor (Commit 3)

**Commit-Vorlage:**

```
refactor(render,card,editor): receive Translations via ctx.t or parameter + drop DE imports

RenderContext erweitert um `t: Translations` (Spec §1.4 Hybrid-Pattern).
flow-renderer + node-renderer lesen ctx.t.* statt DE.*; battery-ring +
card-helpers + editor-list-sections bekommen `t` als Parameter (kein
RenderContext im Pfad). card.ts: @state _lang, willUpdate setzt _lang aus
langFromHass(this.hass), shouldUpdate erkennt Locale-Wechsel (vor return false),
render baut ctx.t = resolveT(this._lang). editor.ts bekommt NEUES willUpdate
(heute nicht vorhanden) für analoges _lang-Set. index.ts: DE-Import gestrichen,
description aus EN.card.description. examples/preview-mocks.ts + preview.html:
Mock-hass.locale.language + UI-Toggle.

Subspec 2026-05-15-en-i18n (v7) §1.4, §2.1, §0.4.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
```

### Task 3.1: `RenderContext` erweitern um `t: Translations`

**Files:**

- Modify: `src/render/context.ts`

- [ ] **Step 1: Import + Feld hinzufügen**

```ts
// neuer Import (oben):
import type { Translations } from '../i18n';

// im Interface (am Ende vor schließender }):
export interface RenderContext {
  config: Config;
  // ... bestehende Felder (formatGrouped, activeThresholdW, ...) unverändert
  /** Resolved translations (DE oder EN), bestimmt aus hass.locale.language in card.ts. */
  t: Translations;
}
```

- [ ] **Step 2: typecheck — viele neue Errors in Callern (renderCard wird ohne `t` aufgerufen)**

```bash
pnpm typecheck
```

Erwartet: Errors in `card.ts:152-167` (`renderCard(this._layout, this._flowResult, { ... })`-Objekt fehlt `t`). Das ist OK — wird in Task 3.5 gefixt.

### Task 3.2: `render/flow-renderer.ts` auf `ctx.t.*`

**Files:**

- Modify: `src/render/flow-renderer.ts`

- [ ] **Step 1: `DE`-Import entfernen (Line 2)**

```ts
// alt:
import { DE } from '../i18n/de';

// neu: Zeile löschen (kein Replacement nötig — ctx.t kommt aus context.ts)
```

- [ ] **Step 2: DE-Verwendungen auf `ctx.t.*` umstellen**

```ts
// Line 38 (renderCard aria-label):
// alt: aria-label="${DE.card.name}"
// neu: aria-label="${ctx.t.card.name}"

// Line 53 (renderDiagnostics label):
// alt: const label = `${DE.diagnostics.iconLabel}: ${count} ${DE.diagnostics.pluralize(count)}`;
// neu: const label = `${ctx.t.diagnostics.iconLabel}: ${count} ${ctx.t.diagnostics.pluralize(count)}`;

// Line 86 (renderDiagnostics title):
// alt: <title>${count} ${DE.diagnostics.title}:\n${summary}</title>
// neu: <title>${count} ${ctx.t.diagnostics.title}:\n${summary}</title>
```

- [ ] **Step 3: typecheck — die spezifischen `flow-renderer.ts`-Stellen sind jetzt OK; restliche Errors bleiben bis Task 3.5**

### Task 3.3: `render/node-renderer.ts` auf `ctx.t.*` (inkl. `nodeName`)

**Files:**

- Modify: `src/render/node-renderer.ts`

- [ ] **Step 1: `DE`-Import entfernen (Line 2)**

```ts
// Zeile löschen: import { DE } from '../i18n/de';
```

- [ ] **Step 2: `renderNode` Line 45 (sensorUnavailable)**

```ts
// alt:
const ariaLabel = unavailable
  ? `${name}: ${DE.states.sensorUnavailable}`
  : ...

// neu:
const ariaLabel = unavailable
  ? `${name}: ${ctx.t.states.sensorUnavailable}`
  : ...
```

- [ ] **Step 3: `nodeName` (Line 212-227) komplett umstellen — inkl. `entry.name`-Fallback**

```ts
// alt:
export function nodeName(node: LayoutNode, ctx: RenderContext): string {
  const entry = configEntryForNode(node, ctx);
  if (entry?.name) return entry.name;
  switch (node.kind) {
    case 'pv':
      return `${DE.nodes.solar} ${node.id}`;
    case 'battery':
      return `${DE.nodes.battery} ${node.id}`;
    case 'grid':
      return DE.nodes.grid;
    case 'home':
      return ctx.config.home?.name ?? DE.nodes.home;
    case 'consumer':
      return `${DE.nodes.consumer} ${node.id}`;
  }
}

// neu:
export function nodeName(node: LayoutNode, ctx: RenderContext): string {
  const entry = configEntryForNode(node, ctx);
  if (entry?.name) return entry.name;
  // unassigned-group hat id === 'g_unassigned' (Subspec §2.2, derive-display-consumers.ts:72)
  if (node.kind === 'consumer' && node.id === 'g_unassigned') {
    return ctx.t.nodes.unassignedGroup;
  }
  switch (node.kind) {
    case 'pv':
      return `${ctx.t.nodes.solar} ${node.id}`;
    case 'battery':
      return `${ctx.t.nodes.battery} ${node.id}`;
    case 'grid':
      return ctx.t.nodes.grid;
    case 'home':
      return ctx.config.home?.name ?? ctx.t.nodes.home;
    case 'consumer':
      return `${ctx.t.nodes.consumer} ${node.id}`;
  }
}
```

**Hintergrund:** Die `unassigned`-Erkennung läuft heute über `entry.name === DE.nodes.unassignedGroup` (String-Vergleich). Nach Phase 2 ist `entry.name === undefined` für die unassigned-Gruppe. Das obige Snippet nutzt `node.id === 'g_unassigned'` als robustesten Diskriminator (P2-F1-Fix), weil `derive-display-consumers.ts:72` diese ID fest setzt — kein String-Vergleich, kein Map-Lookup, kein `entry`-Optional-Chain nötig.

### Task 3.4: `render/battery-ring.ts` mit `t: Translations`-Parameter

**Files:**

- Modify: `src/render/battery-ring.ts`
- Modify: `src/render/battery-ring.test.ts`

- [ ] **Step 1: `DE`-Import entfernen (Line 2), `Translations`-Import + Parameter ergänzen**

```ts
// alt (Line 1-2):
import { svg, type SVGTemplateResult } from 'lit';
import { DE } from '../i18n/de';

// neu:
import { svg, type SVGTemplateResult } from 'lit';
import type { Translations } from '../i18n';
```

- [ ] **Step 2: `formatSocPct` und `renderBatteryRing` Signaturen erweitern**

```ts
// alt:
export function formatSocPct(socPct: number): string {
  return `${Math.round(clampSoc(socPct))} ${DE.units.percent}`;
}

export function renderBatteryRing(socPct: number, color: string): SVGTemplateResult {
  const clamped = clampSoc(socPct);
  const label = svg`<text ...>${formatSocPct(clamped)}</text>`;
  // ...
}

// neu:
export function formatSocPct(socPct: number, t: Translations): string {
  return `${Math.round(clampSoc(socPct))} ${t.units.percent}`;
}

export function renderBatteryRing(
  socPct: number,
  color: string,
  t: Translations,
): SVGTemplateResult {
  const clamped = clampSoc(socPct);
  const label = svg`<text ...>${formatSocPct(clamped, t)}</text>`;
  // ...
}
```

**Parameter-Count-Check (Conventions §1.5):** `renderBatteryRing` hat heute 2 Parameter, nach Refactor 3 — unter Limit 4. `formatSocPct` heute 1, neu 2 — OK.

- [ ] **Step 3: `battery-ring.test.ts` Aufrufe anpassen**

```ts
// Tests bekommen T = DE als Parameter:
import { DE } from '../i18n/de';

// in Tests:
formatSocPct(50, DE);
renderBatteryRing(socPct, color, DE);
```

Der Late-Import `await import('../i18n/de')` in Test Line 134 bleibt unverändert (Test prüft DE-Pfad).

- [ ] **Step 4: Tests ausführen — battery-ring-Tests müssen grün**

```bash
pnpm test src/render/battery-ring.test.ts
```

### Task 3.5: `card.ts` — `_lang`, `shouldUpdate`, `willUpdate`, `render` mit `ctx.t`

**Files:**

- Modify: `src/card.ts`

- [ ] **Step 1: Imports anpassen**

```ts
// alt (Line 14):
import { DE } from './i18n/de';

// neu:
import { langFromHass, resolveT, type Lang } from './i18n';
```

- [ ] **Step 2: `@state _lang` hinzufügen (nach Line 38, in der State-Liste)**

```ts
// neue Zeile (nach _containerW):
@state() private _lang: Lang = 'en';
```

Default `'en'` (Spec-Fallback). Wird in `willUpdate` korrigiert sobald `hass` da ist.

- [ ] **Step 3: `shouldUpdate` Locale-Vergleich VOR `return false` ergänzen (Line 92-99)**

```ts
// alt:
protected override shouldUpdate(changed: PropertyValues): boolean {
  if (changed.size === 1 && changed.has('hass') && this._config) {
    const prev = changed.get('hass') as HomeAssistant | undefined;
    if (!hassRelevantSensorsChanged(prev, this.hass, this._config)) return false;
  }
  return true;
}

// neu:
protected override shouldUpdate(changed: PropertyValues): boolean {
  if (changed.size === 1 && changed.has('hass') && this._config) {
    const prev = changed.get('hass') as HomeAssistant | undefined;
    // Locale-Wechsel MUSS re-render triggern (Subspec §0.4)
    if (prev?.locale?.language !== this.hass?.locale?.language) return true;
    if (!hassRelevantSensorsChanged(prev, this.hass, this._config)) return false;
  }
  return true;
}
```

- [ ] **Step 4: `willUpdate` setzt `_lang` (Line 101-126, am Anfang)**

```ts
// alt:
protected override willUpdate(changed: PropertyValues): void {
  if (!this._config || !this.hass) return;
  if (isStubConfig(this._config)) return;
  ...

// neu:
protected override willUpdate(changed: PropertyValues): void {
  // Locale-Resolution ist unabhängig von Config-Status: stub/empty config darf
  // Sprache erkennen für Skeleton/StubHint-Strings.
  this._lang = langFromHass(this.hass);

  if (!this._config || !this.hass) return;
  if (isStubConfig(this._config)) return;
  ...
```

- [ ] **Step 5: `render()` — `ctx.t` aufbauen und an `renderCard` weiterreichen + 3 inline-Strings auf `T.*` umstellen**

```ts
// alt (Line 128-171):
override render(): TemplateResult {
  if (this._renderError) {
    return html`<ha-card>...
      ${DE.states.cardError}: ${this._renderError}
    ...`;
  }
  if (!this.hass || !this._config) {
    return html`<ha-card>${renderSkeleton()}</ha-card>`;
  }
  if (isStubConfig(this._config)) {
    return html`<ha-card><div class="stub-hint">${DE.states.stubHint}</div></ha-card>`;
  }
  if (!this._flowResult || !this._layout) {
    return html`<ha-card>${renderSkeleton()}</ha-card>`;
  }
  const display = this._config.display ?? {};
  const narrow = this._containerW < 280;
  return html`
    <ha-card>
      ${narrow ? html`<div class="narrow-banner" role="status">${DE.states.narrowBanner}</div>` : ''}
      ${renderCard(this._layout, this._flowResult, {
        config: this._config,
        // ... bestehende Felder
      })}
    </ha-card>
  `;
}

// neu:
override render(): TemplateResult {
  const t = resolveT(this._lang);
  if (this._renderError) {
    return html`<ha-card>...
      ${t.states.cardError}: ${this._renderError}
    ...`;
  }
  if (!this.hass || !this._config) {
    return html`<ha-card>${renderSkeleton(t)}</ha-card>`;
  }
  if (isStubConfig(this._config)) {
    return html`<ha-card><div class="stub-hint">${t.states.stubHint}</div></ha-card>`;
  }
  if (!this._flowResult || !this._layout) {
    return html`<ha-card>${renderSkeleton(t)}</ha-card>`;
  }
  const display = this._config.display ?? {};
  const narrow = this._containerW < 280;
  return html`
    <ha-card>
      ${narrow ? html`<div class="narrow-banner" role="status">${t.states.narrowBanner}</div>` : ''}
      ${renderCard(this._layout, this._flowResult, {
        config: this._config,
        formatGrouped: (display.number_format ?? DEFAULTS.number_format) === 'grouped',
        activeThresholdW: display.active_threshold_w ?? DEFAULTS.active_threshold_w,
        showInactive: display.show_inactive_paths ?? DEFAULTS.show_inactive_paths,
        theme: { colorOverrides: display.colors },
        animation: display.animation,
        buildWarnings: this._buildWarnings,
        unavailableEntities: this._unavailable,
        batterySoc: this._batterySoc,
        displayConsumers: this._displayConsumers,
        unavailableGroups: this._unavailableGroups,
        onNodeClick: (id) => {
          const entity = resolveEntityId(this._config, id, this._displayConsumers);
          if (entity) fireMoreInfo(this, entity);
        },
        t,                     // NEU (Pass 2 P2-F3: vollständige Felder-Liste, kein Auslassungspunkt)
      })}
    </ha-card>
  `;
}
```

- [ ] **Step 6: typecheck — sollte jetzt für card.ts + render/-Module grün sein**

```bash
pnpm typecheck
```

Erwartet: noch Errors in `editor.ts`, `card-helpers.ts`, `editor-list-sections.ts`, `index.ts`. Werden in Tasks 3.6–3.9 gefixt.

- [ ] **Step 7: LOC-Check `card.ts`**

```bash
wc -l src/card.ts
```

Erwartet: ≤ 200. Wenn größer: Lang-Resolution in `card-helpers.ts` auslagern.

### Task 3.6: `card-helpers.ts` — `renderSkeleton(t)`

**Files:**

- Modify: `src/card-helpers.ts`

- [ ] **Step 1: Import-Umstellung + `renderSkeleton`-Signatur**

```ts
// alt (Line 4):
import { DE } from './i18n/de';

// neu:
import type { Translations } from './i18n';

// alt:
export function renderSkeleton(): TemplateResult {
  return html`<div class="loading" aria-busy="true">${DE.states.loading}</div>
    ...`;
}

// neu:
export function renderSkeleton(t: Translations): TemplateResult {
  return html`<div class="loading" aria-busy="true">${t.states.loading}</div>
    ...`;
}
```

- [ ] **Step 2: `card-helpers.test.ts` Aufrufe anpassen**

```ts
import { DE } from './i18n/de';

// in Tests:
renderSkeleton(DE);
```

### Task 3.7: `editor.ts` — NEUES `willUpdate`, `_lang`, `render` mit `t`

**Files:**

- Modify: `src/editor.ts`

- [ ] **Step 1: Imports anpassen**

```ts
// alt (Line 11):
import { DE } from './i18n/de';

// neu:
import { langFromHass, resolveT, type Lang, type Translations } from './i18n';
```

- [ ] **Step 2: `@state _lang` ergänzen + NEUES `willUpdate` (Editor hat heute keins)**

```ts
// nach _validationError (Line 30):
@state()
private _lang: Lang = 'en';

// Neue Lifecycle-Methode (zwischen setConfig und render):
protected override willUpdate(_changed: PropertyValues): void {
  // Lit ignoriert Property-Setzungen auf gleichen Wert (hasChanged default) — kein Re-render-Loop.
  // changed-Parameter ungenutzt, weil hass-Locale unabhängig von Config-Changes zu prüfen ist.
  this._lang = langFromHass(this.hass);
}
```

Import `PropertyValues` ergänzen:

```ts
// Line 1:
import { LitElement, css, html, type PropertyValues, type TemplateResult } from 'lit';
```

- [ ] **Step 3: `render()` — `t` resolven + alle DE-Stellen umstellen**

```ts
override render(): TemplateResult {
  const t = resolveT(this._lang);
  const c = this._config;
  if (!c) return html``;
  return html`
    ${this._validationError ? html`...` : ''}
    ${this._renderGeneral(t)}
    ${renderSolarSection({
      solar: c.solar,
      hass: this.hass,
      t,
      handlers: {
        onItemChange: (i, value) => this._onSolarItemChange(i, value),
        onAdd: () => this._addSolar(),
        onRemove: (i) => this._removeSolar(i),
        onMove: (i, delta) => this._moveSolar(i, delta),
      },
    })}
    ${renderBatterySection({
      battery: c.battery,
      solar: c.solar,
      hass: this.hass,
      t,
      handlers: {
        onItemChange: (i, value) => this._onBatteryItemChange(i, value),
        onPairChange: (i, charged_by) => this._onBatteryPairChange(i, charged_by),
        onModeChange: (i, mode) => this._onBatteryModeChange(i, mode),
        onAdd: () => this._addBattery(),
        onRemove: (i) => this._removeBattery(i),
        onMove: (i, delta) => this._moveBattery(i, delta),
      },
    })}
    ${this._renderGridSection(t)}
    ${renderConsumersSection({
      consumers: c.consumers,
      hass: this.hass,
      t,
      handlers: {
        onItemChange: (i, value) => this._onConsumerItemChange(i, value),
        onAdd: () => this._addConsumer(),
        onRemove: (i) => this._removeConsumer(i),
        onMove: (i, delta) => this._moveConsumer(i, delta),
      },
    })}
  `;
}
```

- [ ] **Step 4: `_renderGeneral` Signatur + Body**

```ts
// alt:
private _renderGeneral(): TemplateResult {
  // ... DE.editor.consumerGroupingNone, .consumerGroupingByArea, .consumerGroupingLabel, .sectionGeneral
}

// neu:
private _renderGeneral(t: Translations): TemplateResult {
  // ... t.editor.consumerGroupingNone, etc.
}
```

- [ ] **Step 5: `_renderGridSection` Signatur + Body**

```ts
private _renderGridSection(t: Translations): TemplateResult {
  // ... t.editor.sectionGrid
}
```

- [ ] **Step 6: `editor.ts:364` (`_addConsumer`) bleibt mit `'Verbraucher'`-Hardcoded**

Hintergrund: `'Verbraucher'` ist hier YAML-Initialwert (User kann sofort ändern), NICHT Render-Zeit-i18n. Spec §2.4 Don't-Touch-Liste markiert das als bewusst akzeptiert.

- [ ] **Step 7: LOC-Check**

```bash
wc -l src/editor.ts
```

Erwartet: ≤ 410 (Spec-Toleranz). Heute 405, Wachstum durch `willUpdate` + `_lang` ist ~5 Zeilen — knapp im Rahmen. **STOP** wenn > 410.

### Task 3.8: `editor-list-sections.ts` — `t: Translations`-Parameter zu allen 3 Render-Funktionen

**Files:**

- Modify: `src/editor-list-sections.ts`

- [ ] **Step 1: Import-Umstellung**

```ts
// alt (Line 2):
import { DE } from './i18n/de';

// neu:
import type { Translations } from './i18n';
```

- [ ] **Step 2: Drei Signaturen auf Argument-Objekt-Pattern umbauen (P2-U1, conventions §1.5)**

Alle drei Render-Funktionen bekommen ein einziges `props`-Objekt statt 5 Einzelparameter. Bestehende `*SectionHandlers`-Interfaces bleiben unverändert (sind schon Argument-Objekte) und werden als Feld `handlers` eingehängt.

```ts
export interface SolarSectionProps {
  solar: readonly SolarConfig[];
  hass: HomeAssistant | undefined;
  t: Translations;
  handlers: SolarSectionHandlers;
}

export function renderSolarSection(props: SolarSectionProps): TemplateResult {
  const { solar, hass, t, handlers: h } = props;
  // alle DE.editor.* → t.editor.*
  // DE.nodes.solar → t.nodes.solar
}

export interface BatterySectionProps {
  battery: readonly BatteryConfig[];
  solar: readonly SolarConfig[];
  hass: HomeAssistant | undefined;
  t: Translations;
  handlers: BatterySectionHandlers;
}

export function renderBatterySection(props: BatterySectionProps): TemplateResult {
  const { battery, solar, hass, t, handlers: h } = props;
  // alle DE.editor.* → t.editor.*
}

export interface ConsumersSectionProps {
  consumers: readonly ConsumerConfig[];
  hass: HomeAssistant | undefined;
  t: Translations;
  handlers: ConsumerSectionHandlers;
}

export function renderConsumersSection(props: ConsumersSectionProps): TemplateResult {
  const { consumers, hass, t, handlers: h } = props;
  // alle DE.editor.* → t.editor.*
}
```

**Parameter-Count-Check (Conventions §1.5):** alle drei Funktionen 1 Parameter (`props`) — konform. **Konsequenz für `editor.ts.render()` (Task 3.7 Step 3):** Call-Sites werden zu `renderSolarSection({ solar: c.solar, hass: this.hass, t, handlers: { /*...*/ } })` etc. — entsprechend in Task 3.7 angepasst.

- [ ] **Step 3: Im Body alle `DE.editor.*`/`DE.nodes.*` durch `t.editor.*`/`t.nodes.*` ersetzen**

```bash
# Stellen (verifizierbar via grep):
# Line 45: ${DE.editor.sectionSolar} → ${t.editor.sectionSolar}
# Line 59: ${DE.editor.moveUp}        → ${t.editor.moveUp}
# Line 62: ${DE.editor.moveDown}      → ${t.editor.moveDown}
# Line 64: ${DE.editor.remove}        → ${t.editor.remove}
# Line 68: ${DE.editor.addSolar}      → ${t.editor.addSolar}
# Line 81: ${DE.editor.sectionBattery}→ ${t.editor.sectionBattery}
# Line 163: ${DE.editor.chargedBy}    → ${t.editor.chargedBy}
# Line 169: ${DE.editor.chargedByPlaceholder} → ${t.editor.chargedByPlaceholder}
# Line 174: `${DE.nodes.solar} ${s.id}` → `${t.nodes.solar} ${s.id}`
# Line 180: ${DE.editor.pairingMissing(item.charged_by)} → ${t.editor.pairingMissing(item.charged_by)}
# Line 185-186: moveUp/moveDown analog
# Line 190: ${DE.editor.remove}      → ${t.editor.remove}
# Line 194: ${DE.editor.addBattery}  → ${t.editor.addBattery}
# Line 215: ${DE.editor.sectionConsumers} → ${t.editor.sectionConsumers}
# Line 227-232: moveUp/moveDown/remove analog
# Line 236: ${DE.editor.addConsumer} → ${t.editor.addConsumer}
```

- [ ] **Step 4: `editor-list-sections.test.ts` Aufrufe auf Argument-Objekt-Pattern umstellen**

```ts
import { DE } from './i18n/de';

// Tests:
renderSolarSection({ solar, hass, t: DE, handlers });
renderBatterySection({ battery, solar, hass, t: DE, handlers });
renderConsumersSection({ consumers, hass, t: DE, handlers });
```

### Task 3.9: `index.ts` — `DE` raus, `EN.card.description` rein

**Files:**

- Modify: `src/index.ts`

- [ ] **Step 1: Import-Tausch (Line 4)**

```ts
// alt:
import { DE } from './i18n/de';

// neu:
import { EN } from './i18n/en';
```

- [ ] **Step 2: Line 33 anpassen**

```ts
// alt:
description: DE.card.description,

// neu:
description: EN.card.description,
```

`CARD_NAME` aus `const.ts:5` bleibt unverändert in Line 32 (Single-Source ADR-0010).

### Task 3.10: `examples/preview-mocks.ts` — `buildMockHass` mit `locale`-Feld

**Files:**

- Modify: `examples/preview-mocks.ts`

- [ ] **Step 1: `buildMockHass()`-Funktion erweitern (heute ohne locale)**

```ts
// alt (Line 451):
export function buildMockHass(scenario: MockScenario): {
  // ... bestehender Return-Type
} {
  // ... return-Objekt OHNE locale-Feld
}

// neu:
export function buildMockHass(
  scenario: MockScenario,
  lang: 'de' | 'en' = 'de',
): {
  // ... bestehender Return-Type, plus:
  locale: { language: 'de' | 'en' };
} {
  return {
    // ... bestehende Felder
    locale: { language: lang },
  };
}
```

**Hinweis:** `examples/` ist NICHT in ESLint/typecheck-Scope (siehe Discovery in Spec §0.2.1). Type-Errors hier werden erst beim Sandbox-Build sichtbar.

### Task 3.11: `examples/preview.html` — DE/EN-Toggle

**Files:**

- Modify: `examples/preview.html`

- [ ] **Step 1: Zwei Buttons + Click-Handler einfügen**

```html
<!-- Vor dem <ha-card>-Slot (genaue Stelle vom Planer anhand bestehender Struktur): -->
<div class="lang-toggle" style="position: fixed; top: 10px; right: 10px; z-index: 100;">
  <button data-lang="de" class="lang-btn lang-btn--active">DE</button>
  <button data-lang="en" class="lang-btn">EN</button>
</div>

<script type="module">
  // Lang-Toggle-Logik
  const card = document.querySelector('custom-energy-flow-card');
  document.querySelectorAll('.lang-btn').forEach((btn) => {
    btn.addEventListener('click', (e) => {
      const lang = e.target.getAttribute('data-lang');
      document.querySelectorAll('.lang-btn').forEach((b) => b.classList.remove('lang-btn--active'));
      e.target.classList.add('lang-btn--active');
      // Mock-hass re-build with new lang
      const newHass = buildMockHass(currentScenario, lang);
      card.hass = newHass;
    });
  });
</script>
```

**Hinweis:** Exakte Integration hängt von bestehender Mock-Setup-Struktur in `preview.html` ab. Planer-Freiraum: Toggle muss `hass.locale.language` ändern und Card-Re-Render auslösen.

### Task 3.12: Sprach-Branching-Tests (`card-helpers.test.ts` + `editor-list-sections.test.ts`)

**Files:**

- Modify: `src/card-helpers.test.ts`
- Modify: `src/editor-list-sections.test.ts`

- [ ] **Step 1: `card-helpers.test.ts` — `it.each` für `renderSkeleton`**

```ts
import { resolveT } from './i18n';

describe('renderSkeleton — Sprach-Branching', () => {
  it.each(['de', 'en'] as const)('rendert Loading-Text in %s', (lang) => {
    const T = resolveT(lang);
    const result = renderSkeleton(T);
    expect(JSON.stringify(result)).toContain(T.states.loading);
  });
});
```

- [ ] **Step 2: `editor-list-sections.test.ts` — `it.each` für `renderSolarSection`**

```ts
import { resolveT } from './i18n';

describe('renderSolarSection — Sprach-Branching', () => {
  it.each(['de', 'en'] as const)('rendert Section-Titel in %s', (lang) => {
    const T = resolveT(lang);
    const result = renderSolarSection({
      solar: [],
      hass: undefined,
      t: T,
      handlers: { onItemChange: () => {}, onAdd: () => {}, onRemove: () => {}, onMove: () => {} },
    });
    expect(JSON.stringify(result)).toContain(T.editor.sectionSolar);
  });
});
```

- [ ] **Step 3: Tests grün**

```bash
pnpm test src/card-helpers.test.ts src/editor-list-sections.test.ts
```

### Task 3.13: `pnpm check` muss komplett grün sein

- [ ] **Step 1: Full check**

```bash
pnpm check
```

Erwartet: alle Errors aus Phase 2 (Type-Cascade) sind durch Phase-3-Refactor aufgelöst. **STOP** bei verbleibenden Errors — keine Auto-Disables, sondern Root-Cause-Fix.

- [ ] **Step 2: `pnpm build` — Bundle ≤ 64 KiB**

```bash
pnpm build
```

Erwartet: `dist/custom-energy-flow-card.js` < 65536 Bytes (`BUNDLE_BUDGET_BYTES` aus `kpi.mjs:29`). Wenn > 64 KiB → Bundle-Profiling via `pnpm build:analyze`.

- [ ] **Step 3: Preview manuell testen — DE/EN-Toggle funktioniert**

```bash
pnpm preview
```

In Browser: Sandbox lädt, DE-Button zeigt deutsche Strings, EN-Button schaltet auf englisch. Beide rendern fehlerfrei.

### Task 3.14: Commit 3

- [ ] **Step 1: Commit erstellen**

```bash
git add src/render/context.ts src/render/flow-renderer.ts src/render/node-renderer.ts \
        src/render/battery-ring.ts src/render/battery-ring.test.ts \
        src/card.ts src/card-helpers.ts src/card-helpers.test.ts \
        src/editor.ts src/editor-list-sections.ts src/editor-list-sections.test.ts \
        src/index.ts examples/preview-mocks.ts examples/preview.html
git commit -m "$(cat <<'EOF'
refactor(render,card,editor): receive Translations via ctx.t or parameter + drop DE imports

RenderContext erweitert um `t: Translations` (Spec §1.4 Hybrid-Pattern).
flow-renderer + node-renderer lesen ctx.t.* statt DE.*; battery-ring +
card-helpers + editor-list-sections bekommen `t` als Parameter (kein
RenderContext im Pfad). card.ts: @state _lang, willUpdate setzt _lang aus
langFromHass(this.hass), shouldUpdate erkennt Locale-Wechsel (vor return false),
render baut ctx.t = resolveT(this._lang). editor.ts bekommt NEUES willUpdate
(heute nicht vorhanden) für analoges _lang-Set. index.ts: DE-Import gestrichen,
description aus EN.card.description. examples/preview-mocks.ts + preview.html:
Mock-hass.locale.language + UI-Toggle.

Subspec 2026-05-15-en-i18n (v7) §1.4, §2.1, §0.4.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

- [ ] **Step 2: `pnpm check` nach Commit grün**

**STOP-Condition:** wenn `pnpm check` nach Commit 3 nicht grün ist — Phase nicht als abgeschlossen markieren, Root-Cause-Fix.

---

## Phase 4 — Doku + Release-Bump (Commit 4)

**Commit-Vorlage:**

```
chore(release): bump to 0.14.0 + docs (ADR-0023, hauptspec, CLAUDE.md, conventions, architecture, README)

CARD_VERSION 0.13.0 → 0.14.0 (Minor-Bump für neues Feature).
package.json version 0.13.0 → 0.14.0.

Neuer ADR-0023: i18n via HA-Locale (DE/EN) mit resolveT-Factory.
ADR-Index erweitert.
Hauptspec §1.3/§1.4: i18n aus Out-of-Scope nehmen.
CLAUDE.md Regel 8 sprachneutral.
conventions.md §11+§15 erweitert + i18n-Pflege-Convention.
architecture.md §2 Modulkarte + §4 ADR-Tabelle.
README Changelog 0.14.0 (Breaking Visual Change für non-de User).

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
```

### Task 4.1: ADR-0023 anlegen

**Files:**

- Create: `docs/adr/0023-i18n-via-hass-locale.md`

- [ ] **Step 1: ADR-Stub aus Spec §4.1 übernehmen (1:1)**

Inhalt: siehe Spec §4.1 Code-Block (kompletter ADR mit Status/Datum/Kontext/Entscheidung 1–5/Optionen A–E/Pros-Cons).

### Task 4.2: ADR-Index aktualisieren

**Files:**

- Modify: `docs/adr/README.md`

- [ ] **Step 1: ADR-0023 in Index eintragen**

```diff
+ - [ADR-0023: i18n via HA-Locale (DE/EN) mit resolveT-Factory](./0023-i18n-via-hass-locale.md) — accepted
```

### Task 4.3: Hauptspec-Updates

**Files:**

- Modify: `docs/specs/2026-05-10-custom-energy-flow-card-design.md`

- [ ] **Step 1: §1.3 In-Scope-Bullet ergänzen**

```diff
+ - **Mehrsprachig**: Card und Editor folgen `hass.locale.language` (`de*` → Deutsch,
+   alles andere → Englisch). Kein Config-Eingriff nötig.
```

- [ ] **Step 2: §1.4 Out-of-Scope-Bullet streichen / als aufgelöst markieren**

```diff
- - Internationalisierung (deutsche Default-Strings, mit `name`-Override pro
-   Knoten in der Config; alle Strings sind aber bereits zentral in `i18n/de.ts`
-   abgelegt — siehe §2.7 — sodass v1.x-i18n trivial wird)
+ - ~~Internationalisierung~~ — aufgelöst durch Subspec
+   [`2026-05-15-en-i18n.md`](./2026-05-15-en-i18n.md) (DE/EN; weitere Sprachen
+   bleiben v1.x-Kandidat).
```

### Task 4.4: `CLAUDE.md` Regel 8

**Files:**

- Modify: `CLAUDE.md`

- [ ] **Step 1: Kritische Regel 8 sprachneutral formulieren**

```diff
- 8. **Strings aus `i18n/de.ts`.** Niemals user-facing Strings hardcoded in
-    Templates. (Spec §11.5)
+ 8. **Strings aus `i18n/`.** Niemals user-facing Strings hardcoded in
+    Templates. Caller holen `T = resolveT(lang)` und reichen `T` durch.
+    (Spec §11.5, Subspec 2026-05-15-en-i18n, ADR-0023)
```

### Task 4.5: `docs/conventions.md` §11 + §15 + Pflege-Convention

**Files:**

- Modify: `docs/conventions.md`

- [ ] **Step 1: §11 Anti-Patterns Bullet anpassen**

```diff
- - ❌ Hardcoded User-Strings (immer aus `i18n/de.ts`)
+ - ❌ Hardcoded User-Strings (immer aus `i18n/`, via `resolveT(lang)` aufgelöst — Modul-Singleton verboten)
```

- [ ] **Step 2: §15 Sprache erweitern**

```diff
- - **User-facing Strings**: Deutsch (in `i18n/de.ts` zentralisiert)
+ - **User-facing Strings**: Deutsch (`i18n/de.ts`) + Englisch (`i18n/en.ts`), Sprach-Auswahl
+   automatisch über `hass.locale.language` (`resolveT(lang)`-Factory in `i18n/index.ts`,
+   siehe ADR-0023). Default-Fallback: EN.
```

- [ ] **Step 3: Neuer Pflege-Hinweis in §15 (oder §12 — Planer-Wahl)**

```markdown
**i18n-Pflege:** Bei jeder Änderung an `src/i18n/de.ts` MUSS `src/i18n/en.ts`
synchron mitgepflegt werden. TypeScript verhindert struktur-Drift
(`EN: Translations`), aber Wert-Drift („DE-Bullet hinzugefügt, EN vergessen")
ist Reviewer-Pflicht. `pnpm typecheck` fängt fehlende Keys.
```

- [ ] **Step 4: §3 Datei-Größen-Limit für `editor.ts` (P2-U2)**

```diff
- - **editor.ts** ≤ 400 LOC
+ - **editor.ts** ≤ 410 LOC (Tech-Debt formalisiert seit i18n-Refactor; siehe ADR-0023 Konsequenz)
```

Begründung: `editor.ts` ist heute 405 LOC (vor i18n-Refactor); mit `willUpdate` + `_lang` + `t`-Aufrufen wächst auf ~410. Statt Auslagerung in Helper wird die Toleranz formalisiert (User-Decision P2-U2).

- [ ] **Step 5: `scripts/kpi.mjs` `FILE_LOC_LIMITS` synchron anpassen**

```diff
const FILE_LOC_LIMITS = {
  'src/card.ts': 200,
- 'src/editor.ts': 400,
+ 'src/editor.ts': 410,
  'src/engine/energy-engine.ts': 300,
};
```

`scripts/kpi.mjs:19` enthält die Convention §3 Hard-Gates für KPI-Threshold-Checks. Ohne diese Anpassung schlägt `pnpm kpi:report` post-Refactor fehl ("editor.ts: 410 > 400"). Plan-Task 4.5 schließt sie in den Commit 4 ein (Scope `docs` ist OK weil kpi.mjs Hard-Gate ein Doku-/Konvention-Sync ist; alternativ `build`-Scope).

### Task 4.6: `docs/architecture.md` §2 + §4

**Files:**

- Modify: `docs/architecture.md`

- [ ] **Step 1: §2 Modulkarte `i18n/`-Beschreibung erweitern**

```diff
- i18n/    ←  alle User-Strings (DE)
+ i18n/    ←  alle User-Strings (DE + EN); resolveT(lang) Factory + langFromHass(hass)
```

- [ ] **Step 2: §4 ADR-Tabelle**: ADR-0023 hinzufügen.

### Task 4.7: `const.ts` `CARD_VERSION` 0.14.0

**Files:**

- Modify: `src/const.ts`

- [ ] **Step 1: Version-Bump**

```diff
- export const CARD_VERSION = '0.13.0';
+ export const CARD_VERSION = '0.14.0';
```

### Task 4.8: `package.json` version 0.14.0

**Files:**

- Modify: `package.json`

- [ ] **Step 1: Version-Bump**

```diff
- "version": "0.13.0",
+ "version": "0.14.0",
```

### Task 4.9: `README.md` Changelog 0.14.0

**Files:**

- Modify: `README.md`

- [ ] **Step 1: Changelog-Eintrag oben im Changelog-Bereich einfügen (Spec §5.3 Vorlage)**

```markdown
## 0.14.0 — 2026-05-15

### Englische Sprachunterstützung

- Card und Editor folgen jetzt der Home-Assistant-Sprache (`hass.locale.language`).
- `de` (inkl. `de-DE`, `de-AT`, `de-CH`) → deutsche Texte wie bisher.
- Alle anderen Sprachen → englische Texte.
- Kein Config-Eintrag nötig — wechselt automatisch beim HA-Sprachwechsel.

### Breaking Visual Change

User mit einer Home-Assistant-Sprache ≠ `de` sehen die Card ab v0.14.0 auf
Englisch (vorher Deutsch). Wer beim alten Verhalten bleiben möchte, kann
in den HA-Profil-Einstellungen die Sprache auf Deutsch stellen.

### Was zu tun ist nach Update

1. **Browser-Cache leeren** (Strg+Shift+R), falls die alte Sprache noch erscheint.
2. Keine Config-Anpassung nötig.
```

### Task 4.10: Final-Verifikation

- [ ] `pnpm check` grün
- [ ] `pnpm build` produziert Bundle ≤ `BUNDLE_BUDGET_BYTES` (65536 Bytes = 64 KiB) — erwartet ~62500 Bytes
- [ ] `pnpm smoke` grün — Mock-hass ohne locale fällt korrekt auf EN
- [ ] `wc -l src/card.ts` ≤ 200
- [ ] `wc -l src/editor.ts` ≤ 410 (Spec-Toleranz)
- [ ] Unverändert-Check: `git diff` zeigt keine Änderungen an `src/engine/*`, `src/util/*` (außer `const.ts`), `src/ha/*`, `src/i18n/de.ts`

### Task 4.11: Commit 4

```bash
git add docs/adr/0023-i18n-via-hass-locale.md docs/adr/README.md \
        docs/specs/2026-05-10-custom-energy-flow-card-design.md \
        CLAUDE.md docs/conventions.md docs/architecture.md \
        src/const.ts package.json README.md
git commit -m "$(cat <<'EOF'
chore(release): bump to 0.14.0 + docs (ADR-0023, hauptspec, CLAUDE.md, conventions, architecture, README)

CARD_VERSION 0.13.0 → 0.14.0 (Minor-Bump für neues Feature).
package.json version 0.13.0 → 0.14.0.

Neuer ADR-0023: i18n via HA-Locale (DE/EN) mit resolveT-Factory.
ADR-Index erweitert.
Hauptspec §1.3/§1.4: i18n aus Out-of-Scope nehmen.
CLAUDE.md Regel 8 sprachneutral.
conventions.md §11+§15 erweitert + i18n-Pflege-Convention.
architecture.md §2 Modulkarte + §4 ADR-Tabelle.
README Changelog 0.14.0 (Breaking Visual Change für non-de User).

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Phase 5 — KPI-Post-Snapshot + Code-Review-Workflow (verbindlich)

### Task 5.1: Post-Snapshot

```bash
pnpm test:coverage && pnpm build
pnpm kpi:snapshot --label post-en-i18n --phase post
pnpm kpi:report
```

Erwartet: Delta zeigt:

- Bundle: ~+1 KiB (60.0 → ~61.0 KiB)
- LOC: +~150 (i18n/index.ts, en.ts, Tests; -minor in derive-display-consumers)
- Coverage: stabil ≥ 99 %; `i18n/` neu im Hard-Gate
- `card.ts` LOC: ≤ 200; `editor.ts` LOC: ≤ 410
- Keine neuen `any`/`as`/`!`-Counts

### Task 5.2: Code-Review-Workflow starten

Per CLAUDE.md „Code-Review — Workflow":

- 6-Pass-Loop (sequentiell) mit rotierenden Fokus-Vektoren
- Plan-Komplexität „Mittel" (15–25 Tasks): Pässe 1+2+3+6 (4 Pässe)
- AUTO-FIX inline mit Trust-but-Verify
- USER-DECISION sammeln
- LESSON-LEARNED an `docs/lessons-learned.md` appenden

### Task 5.3: `superpowers:finishing-a-development-branch`

Nach Code-Review-Stop-Kriterium: Merge/Tag/PR/HACS-Bump-Optionen (Skill, nicht autonom).

---

## Self-Review-Checkliste (vor Plan-Abschluss — Hauptagent durchgeht)

- [ ] **Spec-Coverage:** jede Spec-Sektion hat einen Task — Mapping-Tabelle in Phase-Header
- [ ] **Spec-Plan-Alignment:** kein Widerspruch zur Spec (Plan kann Spec ergänzen, aber nicht widersprechen)
- [ ] **Keine Placeholders** (`TBD`/`TODO`/`Similar-to`)
- [ ] **Type-Consistency:** `Translations`, `Lang`, `langFromHass`, `resolveT`, `Widen<T>` durchgängig konsistent benannt
- [ ] **Commit-Granularität:** 4 Commits laut Spec §2.3
- [ ] **Verifikations-Pipeline** pro Phase: typecheck → lint → test → check → build → preview/smoke (wo relevant)
- [ ] **Don't-Touch-Liste** (Spec §0.0 + §0.5) respektiert
- [ ] **Code-Reuse-Tabelle** (Spec §2.4): `Translations`/`Lang`/`langFromHass`/`resolveT`/`RenderContext.t` konsistent referenziert
- [ ] **Anti-Patterns** (Spec §2.4): kein Singleton, kein eigener Lang-Type, kein `as Translations`-Cast
- [ ] **Standing-Reminder pro Phase:** Conventions/ADRs in Phase-Header reproduziert
- [ ] **TDD-Order:** Tests vor Code für `i18n/` (Phase 1.1, 1.2 rot vor 1.3, 1.4)
- [ ] **STOP-Conditions:** kritische Tasks (1.1, 1.4, 2.4, 3.5, 3.7, 3.13) haben STOP-Bedingungen
- [ ] **Framework-Quirks:** Lit-Lifecycle (willUpdate-vs-render), shouldUpdate-Erweiterung, Widen-Mapped-Type
- [ ] **Build-Pipeline** explizit: vitest.config.ts + kpi.mjs Diffs mit Zeile
- [ ] **Doku-Pflicht (conventions §12):** ADR-0023 + ADR-Index + architecture.md §4 (Pflicht bei neuem ADR); README bei User-facing Verhalten

---

## Out of Scope (nicht Teil dieses Plans)

Aus Spec §6 übernommen:

- **Weitere Sprachen (FR, ES, IT, …)** — v1.x-Kandidat. Pattern ist trivial erweiterbar.
- **Config-Override `display.language`** — v1.x-Kandidat.
- **Dynamic-Import / Lazy-Loading** — Bundle-Budget reicht statisch.
- **README-Übersetzung / `README.en.md`** — v1.x-Kandidat (HACS-Listing zeigt deutsches README für englische User; akzeptiert).
- **Default-Knoten-Namen aus `derive-display-consumers.ts:43` (mapNoneMode)** sprachneutralisieren — Out-of-Scope (Spec §2.2), v1.x-Refactor.
- **Editor-Stub-Default `'Verbraucher'` (`editor.ts:364`)** — YAML-Initialwert, kein Render-Zeit-i18n.
- **Reaktiver Sprach-Switch ohne hass-Update** — Lit-Reaktivität reicht.
- **`scripts/smoke-test.mjs` Mock-hass um locale erweitern** — bewusst akzeptiert (Mock ohne locale fällt auf EN, Smoke-Test hat keine sprachabhängigen Assertions).

---

## Notizen für den Implementierer

- **TDD-Order in Phase 1:** Erst `index.test.ts` + `en.test.ts` rot → dann `index.ts` + `en.ts` → grün. Wenn ein Test grün bleibt, der eigentlich rot sein sollte → Test-Bypass-Verdacht → STOP.
- **Phase-2-zu-Phase-3-Transient:** `pnpm typecheck` ist zwischen Commit 2 und Commit 3 erwartungsgemäß rot (Renderer-Cascade). Akzeptabel, weil Commit 3 unmittelbar folgt.
- **Widen-Type-System:** falls `EN: Translations`-Annotation neue Type-Errors zeigt, die nicht durch Widen abgedeckt sind (z.B. nested function-types) — Widen erweitern, nicht `as Translations` casten.
- **Layer-Boundary für `i18n/`:** `i18n/index.ts` darf KEIN HA-Type importieren. Strukturelle `HassLocaleView` ist die einzige Lösung.
- **Editor-Lifecycle:** `editor.ts` bekommt heute KEIN `willUpdate` — Task 3.7 fügt es NEU hinzu. `PropertyValues`-Import nicht vergessen.
- **shouldUpdate-Stelle in `card.ts:92-99`:** Locale-Check VOR `hassRelevantSensorsChanged` einsetzen, sonst wird Locale-Wechsel verschluckt wenn keine Sensoren ändern.
- **Smoke-Test:** läuft mit Mock-hass ohne locale → Card fällt auf EN-Fallback. Keine sprachabhängigen Assertions im Smoke-Test, Convention §5.4 aus Spec.
- **Preview-Sandbox:** UI-Toggle in `preview.html` ermöglicht manuelle Verifikation beider Sprachen. Pflicht-Schritt vor Commit 3.
- **YAML-User-Override-Edge-Case (Spec §5.1):** Wenn ein User in YAML explizit deutsche `name`-Felder gesetzt hat (z.B. `home.name: Hausverbrauch` in `examples/2-pv-2-batt.yaml:30`), bleibt diese Bezeichnung in der UI **deutsch**, auch bei EN-Locale. Renderer-Fallback in `node-renderer.ts:nodeName` (Task 3.3) bevorzugt User-Override (`entry.name`) gegenüber `ctx.t.nodes.home`. Bewusst akzeptiert — kein Code-Eingriff nötig, kein Migrations-Bruch.
